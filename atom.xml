<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://divertingPan.github.io</id>
    <title>老潘家的潘老师</title>
    <updated>2020-08-19T08:20:41.948Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://divertingPan.github.io"/>
    <link rel="self" href="https://divertingPan.github.io/atom.xml"/>
    <subtitle>大型摸鱼现场</subtitle>
    <logo>https://divertingPan.github.io/images/avatar.png</logo>
    <icon>https://divertingPan.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, 老潘家的潘老师</rights>
    <entry>
        <title type="html"><![CDATA[【转载】pytorch里面的天坑]]></title>
        <id>https://divertingPan.github.io/post/Izwv6avfw/</id>
        <link href="https://divertingPan.github.io/post/Izwv6avfw/">
        </link>
        <updated>2020-08-18T13:17:09.000Z</updated>
        <content type="html"><![CDATA[<p>今天随便炼丹取暖，结果卡了一个坑，在网上找到一个解决方案，顺便把其他的部分也粘贴过来存个档吧</p>
<blockquote>
<p>作者：八戒<br>
链接：https://zhuanlan.zhihu.com/p/95883048<br>
来源：知乎<br>
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。</p>
</blockquote>
<h1 id="pytorch-数据读取模块windows和linux不同">Pytorch 数据读取模块windows和linux不同</h1>
<p>报错如下：</p>
<pre><code>AttributeError: Can't pickle local object 'get_loader.&lt;locals&gt;.&lt;lambda&gt;'
</code></pre>
<p>问题如上所示，但是Google全是<a href="https://blog.csdn.net/javali1995/article/details/78475108">这种</a>，或者<a href="https://blog.csdn.net/qq_39314099/article/details/83822593">这种</a>。它还有一个报错是</p>
<pre><code>EOFError: Ran out of input
</code></pre>
<p>又或者<a href="https://blog.51cto.com/itbases/2087664">这种</a>。无解啊，报错的是数据读取模块，但是我的程序在Linux系统下是没有问题的，但是到了Windows系统下就有问题了，不知道为什么。</p>
<pre><code class="language-python">data.DataLoader(dataset=dataset, batch_size=1, shuffle=False, num_workers=num_thread)
</code></pre>
<p>就是把num_workers指定为0，或者直接不写。</p>
<hr>
<h1 id="pytorch-gpu问题">Pytorch GPU问题</h1>
<p>Pytorch在GPU并行方面还算很方便。在定义好model之后只需要使用一行：</p>
<pre><code class="language-python">model = torch.nn.DataParallel(model)
#默认调用所有GPU
</code></pre>
<p>即可实现在所有GPU上并行运算。</p>
<p>但是有时候直接占用所有的GPU是没有必要的，如果要指定GPU，可以在DataParallel中增加一个参数：</p>
<pre><code class="language-python">model = torch.nn.DataParallel(model, device_ids=[0,1])
</code></pre>
<p>在运行时会报出如下错误：</p>
<pre><code>RuntimeError: all tensors must be on devices[0]
</code></pre>
<p>该错误的解决方式有很多，这里推荐使用一种最简单的，在运行代码的时候，调整程序可见的GPU即可，具体而言就是将原先的运行指令：</p>
<pre><code>python train.py
</code></pre>
<p>改为</p>
<pre><code>CUDA_VISIBLE_DEVICES=2,3  python train.py
</code></pre>
<p>而程序中还是写为：</p>
<pre><code class="language-python">model = torch.nn.DataParallel(model, device_ids=[0,1])
</code></pre>
<h1 id="pytorch-权重和数据不对">Pytorch 权重和数据不对</h1>
<p>报错如下：</p>
<pre><code>Input type (CUDAFloatTensor) and weight type (FloatTensor) should be the same
</code></pre>
<p>仔细看错误信息，CUDA和CPU，输入数据x和模型中的权重值类型不一样，一般来说是因为模型的参数不在GPU中，而输入数据在GPU中，通过添加model.cuda()将模型转移到GPU上以解决这个问题。但是我的模型中已经有model.cuda了。</p>
<p>问题在模型中，不能把卷积定义到forward中，像下面这样是不行的</p>
<pre><code class="language-python">def forward(self, x):
    fin_x = nn.Conv2d(128,1,1,1)(x)
</code></pre>
<p>这就会导致这个问题，必须把</p>
<pre><code class="language-python">fin_x = nn.Conv2d(128,1,1,1)(x)
</code></pre>
<p>这个定义到上面的模型中</p>
<h1 id="pytorch-cpu加载多gpu训练模型">Pytorch cpu加载多GPU训练模型</h1>
<p>多GPU训练保存的模型会多一个module</p>
<pre><code>Missing key(s) in state_dict: &quot;layer1.0.0.weight&quot;, &quot;layer1.0.2.weight&quot;, 
Unexpected key(s) in state_dict: &quot;module.layer1.0.0.weight&quot;,
</code></pre>
<p>怎么解决呢？</p>
<pre><code class="language-python">    state_dict = torch.load(model_path,map_location='cpu')
    from collections import OrderedDict
    new_state_dict = OrderedDict()
    for k, v in state_dict.items():
        name = k[7:]                  # remove module.
        new_state_dict[name] = v
    net.load_state_dict(new_state_dict)
</code></pre>
<h1 id="torchvision中datasets加载cifar10cifar100问题">torchvision中datasets加载CIFAR10,CIFAR100问题</h1>
<p>因为网络的问题，自己就提前下载了数据集，但是网络一加载就报错，找不到数据，我就奇了怪了。。。。明明有就是找不到</p>
<pre><code class="language-python">train_set = datasets.CIFAR10(data, train=True, transform=train_transforms, download=False) 
test_set = datasets.CIFAR10(data, train=False, transform=test_transforms, download=False)
</code></pre>
<p>报错如下：</p>
<pre><code>RuntimeError: Dataset not found or corrupted. You can use download=True to download it
</code></pre>
<p>几经折腾才发现问题在那，自己提前下载了，**参数要给全。。。**路径给全，也就是绝对路径</p>
<pre><code class="language-python">train_set = datasets.CIFAR10(root=data, train=True, transform=train_transforms, download=False)  
test_set = datasets.CIFAR10(root=data, train=False, transform=test_transforms, download=False)
</code></pre>
<h1 id="pytorch中cudnn错误">Pytorch中cudnn错误</h1>
<pre><code>RuntimeError: cuDNN error: CUDNN_STATUS_NOT_SUPPORTED. This error may appear if you passed in a non-contiguous input.
</code></pre>
<p>看官方说法是矩阵太大了，然后把批量换小点，我换小了也不行，只有换到1才行。。。。</p>
<p>还有就是禁用cudnn的加速</p>
<pre><code class="language-python">torch.backends.cudnn.enabled = False
</code></pre>
<p><strong>这个方法是可行的，但是慢的一批</strong></p>
<h1 id="pycharm-数据显示不全">Pycharm 数据显示不全</h1>
<p>在Anaconda中notebook可以显示全部的数据，但是在pycharm中，使用print(dataframe)，不能显示dataframe的全部数据，中间的数据用省略号表示。</p>
<p>在pycharm中显示全部数据解决方法，输入下面代码：</p>
<pre><code class="language-python">import pandas as pd 
#显示所有列
pd.set_option('display.max_columns', None)
#显示所有行
pd.set_options('display.max_rows', None)
</code></pre>
<p>类似的，对于numpy array print后不能完全显示输入下面代码：</p>
<pre><code class="language-python">numpy.set_printoptions(threshold = np.inf)

#若想不以科学计数显示:
numpy.set_printoptions(suppress = True)
</code></pre>
<h1 id="numpy问题">numpy问题</h1>
<p>报错如下：</p>
<pre><code>in numpy.import_array
ImportError: numpy.core.multiarray failed to import
</code></pre>
<p>这个问题可能有两个原因</p>
<ol>
<li>你用conda装了一个numpy,又用pip装了一个numpy。导致冲突，你卸载一个就行了；比如：</li>
</ol>
<pre><code>conda uninstall numpy
</code></pre>
<ol start="2">
<li>numpy 版本太低，导致不兼容，更新一下</li>
</ol>
<pre><code>pip install -U numpy
conda update numpy
</code></pre>
<h1 id="pip问题">pip问题</h1>
<p>装sklean的时候发现如下错误</p>
<pre><code>ERROR: Could not find a version that satisfies the requirement threadpoolctl&gt;=2.0.0 (from scikit-learn-&gt;sklearn) (from versions: 1.0.0, 1.1.0)
ERROR: No matching distribution found for threadpoolctl&gt;=2.0.0 (from scikit-learn-&gt;sklearn)
</code></pre>
<p>然后上网找了下解决方案，更新pip</p>
<pre><code>python -m pip install --upgrade pip
</code></pre>
<p>然后 然后 然后就悲剧了！！！我的pip就崩了</p>
<pre><code>Script file 'D:\Anaconda3\Scripts\pip-script.py' is not present
</code></pre>
<p><strong>大家更新的时候一定要在管理员的情况下更新你的pip</strong></p>
<p><strong>然后管理员运行</strong></p>
<pre><code>easy_install pip
</code></pre>
<p>解决上面的问题</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【5分钟实例】去除大量图片中重复的图片]]></title>
        <id>https://divertingPan.github.io/post/remove_duplicate/</id>
        <link href="https://divertingPan.github.io/post/remove_duplicate/">
        </link>
        <updated>2020-08-17T07:47:20.000Z</updated>
        <content type="html"><![CDATA[<h1 id="需求假设">需求假设</h1>
<p>你的老板给你<code>上万张</code>候选人的证件照，但是由于某些原因，一部分人的照片有不止一张（重复了），让你把重复出现的照片删掉，下班之前整理好发给他。你一看表距离下班只剩<code>5分钟</code>了，加班是不可能加班的。<br>
<img src="https://divertingPan.github.io/post-images/1597651150468.PNG" alt="" loading="lazy"><br>
这时候你灵光一现，想起了python有图像处理模块</p>
<h1 id="只需5分钟的操作">只需5分钟的操作</h1>
<p>拿起键盘打开你最爱的IDE二话不说首先import以下必定要用到的库</p>
<pre><code class="language-python">import cv2
import numpy as np
import os
</code></pre>
<p>别急，这种任务网上肯定是有现成轮子的，直接借鉴就好。对于图像去重，主要思路有，md5比较法和感知哈希法。其中md5比较法的鲁棒性稍微低了点，有时候图片因为多次压制或其他因素导致md5变化，从而图片看起来一样，但是会被判做不同图片。</p>
<p>感知哈希算法是比较常用的方法，在硬币分类检测时老潘也尝试过，但是并不适用于那个场景。但是今天的场景是绝对适合的。其中感知哈希算法又可以细分出平均哈希（aHash），感知哈希（pHash），差异值哈希（dHash）三种。硬币分类里使用了pHash，对于这三种的对比，在一篇CSDN博文<a href="https://blog.csdn.net/qq_32799915/article/details/81000437">感知哈希 ，平均哈希，差异值哈希</a>里面有详细的讲解和对比实验。</p>
<p>这里老潘在网上找到的一个现成的图片去重的脚本使用的是aHash</p>
<pre><code class="language-python">img = cv2.resize(img,(8,8))
avg_np = np.mean(img)
img = np.where(img&gt;avg_np,1,0)
hash_dic[img_name] = img
</code></pre>
<p>读取每个图片，计算出他的aHash值，如果记录表里没有这个值，就把他和他的值存进去，如果有，证明重复，删掉这个图即可。思路很简单。</p>
<p>当然，这里并不是直接检测有没有一模一样的aHash值，而是检测有没有汉明距离小于某个值的，以防两个一样的图片因为微小的差异被漏检。</p>
<p>顺手再添加一个功能，有些人的照片可能损坏了或者夹杂了奇奇怪怪的不是图片的文件。我们把这种也剔除掉。</p>
<pre><code class="language-python">try:
    img= cv2.imread(os.path.join(path, img_name))
    img= cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
except:
    os.remove(os.path.join(path, img_name))
    print('wrong file')
    continue
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597651168140.PNG" alt="" loading="lazy"></figure>
<p>复制粘贴代码1秒钟，4分59秒钟用来运行代码。将筛选好的图片给正在加班的程序员，下班回家。</p>
<h1 id="附录1">附录1</h1>
<p>aHash到底可靠不可靠？会不会有误检和漏检？以这个现成脚本代码为例，做几个尝试性的实验看看。</p>
<p>实验1：普通去重，实验前后的图片集合如图所示。<br>
<img src="https://divertingPan.github.io/post-images/1597651178686.PNG" alt="" loading="lazy"></p>
<p>实验2：相似图片（我真的觉得这几位差不多，如有冒犯，狗妹纳赛）<br>
<img src="https://divertingPan.github.io/post-images/1597651185463.PNG" alt="" loading="lazy"></p>
<p>实验3：相同图片不同饱和度（反正图片读进来都是要先转成灰度的，所以改变饱和度确实不会对改变aHash值有什么作用）<br>
<img src="https://divertingPan.github.io/post-images/1597651191249.PNG" alt="" loading="lazy"></p>
<h1 id="附录2">附录2</h1>
<p>完整的开箱即用代码，也可以<a href="https://github.com/divertingPan/utility_room/blob/master/remove_duplicate.py">到github里面下载</a></p>
<pre><code class="language-python">'''
https://www.jianshu.com/p/5a7daa2123a8
'''
 
import cv2
import numpy as np
import os
 
 
if __name__ == '__main__':
   path = './anim'
   
   img_list = os.listdir(path)
   hash_list = []
   for ind, img_name in enumerate(img_list):
       print('checking:', ind+1, 'from total:', len(img_list))
       try:
           img = cv2.imread(os.path.join(path, img_name))
           img = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
       except:
           os.remove(os.path.join(path, img_name))
           print('wrong file')
           continue
 
       img = cv2.resize(img,(8,8))
 
       avg_np = np.mean(img)
       img = np.where(img&gt;avg_np,1,0)
       if len(hash_list)&lt;1:
           hash_list.append(img)
       else:
           for i in hash_list:
                flag = True
                dis = np.bitwise_xor(i,img)
 
                if np.sum(dis) &lt; 5:
                    flag = False
                   os.remove(os.path.join(path, img_name))
                    print('remove')
                    break
           if flag:
                hash_list.append(img)

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【5分钟实例】按名字从大量简历中筛出需要的简历]]></title>
        <id>https://divertingPan.github.io/post/pick_resume/</id>
        <link href="https://divertingPan.github.io/post/pick_resume/">
        </link>
        <updated>2020-06-24T08:06:51.000Z</updated>
        <content type="html"><![CDATA[<p><del>水一篇。</del>开个新坑，本系列重点打造能够在5分钟内完成的又简单又实用的实例。今天以办公场景比较常见的一个需求为例。具体场景如下。</p>
<h1 id="需求假设">需求假设</h1>
<p>你的老板给你<code>上万份</code>候选人交的简历，又给了你一个Excel表格，让你把表格里的人的简历挑出来，下班之前发给他。你一看表距离下班只剩<code>5分钟</code>了，加班是不可能加班的。</p>
<p>这时候你定睛一看，所有的简历文件名里面都包含人名。</p>
<h1 id="只需5分钟的操作">只需5分钟的操作</h1>
<p>拿起键盘打开你最爱的IDE二话不说首先import以下必定要用到的库</p>
<pre><code class="language-python">import pandas as pd
import os
from shutil import copyfile
</code></pre>
<p>打开Excel表格看一眼，找到“姓名”所在的列<br>
<img src="https://divertingPan.github.io/post-images/1597652772497.webp" alt="" loading="lazy"></p>
<p>是第一列，所以直接读第一列进来即可</p>
<pre><code class="language-python">df = pd.read_excel(menu_path, usecols=[0])
</code></pre>
<p>之后去遍历存放简历的文件夹下的所有简历文件</p>
<pre><code class="language-python">for maindir, subdir, filename in os.walk(resume_path):
    for i, file in enumerate(filename):
</code></pre>
<p>对每个遍历到的文件，先去除文件名里面的空格，以免两个字的人名中间有空格，干扰识别。但是又不能直接把变量file给改变，否则后面复制文件时会找不到文件。</p>
<pre><code class="language-python">file_no_space = ''.join(file.split())
</code></pre>
<p>这时候再遍历读进来的Excel的人名信息，逐条检查是否能和这个文件名匹配上人名。若可以匹配到，那就把这个文件复制走。</p>
<pre><code class="language-python">for key in df['姓名']:
    if key in file_no_space:
        copyfile(os.path.join(maindir, file), os.path.join(destination_path, file))
        print(file)
</code></pre>
<p>拓展功能，如果想要把顺序也排好，那就只需记录一下目前匹配到的情况下，这个人名在Excel表格里的序号。可以对上面这一块代码做以下改动。首先使用enumerate()迭代df['姓名']，这样可以启用循环时的索引计数（我不知道这到底叫什么，我就这样称呼了），之后输出文件时将这个索引加在文件名最开头，就能实现按照Excel里面的人名顺序排序简历。</p>
<pre><code class="language-python">for i, key in enumerate(df['姓名']):
    if key in file_no_space:
        copyfile(os.path.join(maindir, file), os.path.join(destination_path, str(i)+file))
        print(file)
</code></pre>
<p>结束。看一下效果，证明我不是在胡扯，是真的可以用。文件标号1缺失了，顺便还能发现有人没交简历。看一眼时间离下班还剩5分01秒。<br>
<img src="https://divertingPan.github.io/post-images/1597653189483.png" alt="" loading="lazy"></p>
<p>对于重名的问题，可以按照“学号”，或者“身份证号”等其他不易重复的值作为匹配的键值。但前提是要让简历名字里包含这些信息。</p>
<h1 id="附录">附录</h1>
<p>全部代码如下。或者<a href="https://github.com/divertingPan/utility_room/blob/master/pick_resume.py">到github下载</a>。</p>
<pre><code class="language-python"># -*- coding: utf-8 -*-
import pandas as pd
import os
from shutil import copyfile


resume_path = './resume'
menu_path = './1组分组名单.xlsx'
destination_path = './sorted'


if not os.path.exists(destination_path):
    os.makedirs(destination_path)

df = pd.read_excel(menu_path, usecols=[0]) # 以第1列（人名）作为检索key

for maindir, subdir, filename in os.walk(resume_path):
    for i, file in enumerate(filename):
        file_no_space = ''.join(file.split())
        for i, key in enumerate(df['姓名']):
            if key in file_no_space:
                copyfile(os.path.join(maindir, file), os.path.join(destination_path, str(i)+file))
                print(file)

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[高阶 | 使用神经网络对网络资源自动分类——以校花贴吧图片为例]]></title>
        <id>https://divertingPan.github.io/post/cnn_classifier/</id>
        <link href="https://divertingPan.github.io/post/cnn_classifier/">
        </link>
        <updated>2019-09-28T07:52:59.000Z</updated>
        <content type="html"><![CDATA[<p>本来这一篇应该在两个月之前就写完的，但是众所周知<code>在家=生产力0</code>这样一条定律限制了老潘。所以在大量粉丝取关之际，赶紧把这篇推送搞上来，<del>诚挚为已经取关的同学没法看完本系列作而深感遗憾。</del></p>
<p>本篇继承“高效获取互联网资源”这一主题，并引进目前非常时髦的神经网络技术，实现前文<a href="https://divertingpan.github.io/post/python_tieba/">进阶 | 利用Python开挂式获取网络资源——以搜集贴吧图片为例</a>的后续功能开发，为广大<del>挂逼</del>网友创造自动分类校花与水图表情包的自动分拣机。</p>
<h1 id="摘要">摘要</h1>
<p>本项目以百度贴吧众多图片作为待分类数据，通过最少的操作步骤，实现了在Keras环境下使用以ImageNet为预训练权重的ResNet50网络进行图片分类。分类效果比较好甚至不需要对模型进行什么改动，最终实现了从杂乱图片中筛选包含目标主体的图片。针对包含人体人像这类图片，调用了Face++进行识别，做到了识别图片中是否包含人像和人像的性别<del>颜值打分（并没有）</del>评判。项目整体准确度一般，只能作为娱乐项目玩一玩。</p>
<h1 id="绪论">绪论</h1>
<p>当某同学阅读完本系列前一作，兴致勃勃的把程序<strong>复制-粘贴</strong>下来之后，准备上贴吧去抓取校花，却不料被现实绊倒，迷茫在表情包的海洋中。</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597824112149.PNG" alt="" loading="lazy"></figure>
<p>虽然可以<del>像一个痴汉</del>去手动地一个一个检查哪个图片是美女哪个图片是表情包，但是一来会让同学们身体被掏空，二来也丧失了高效获取信息的初心。所以为了使各位同学身体健康心情愉悦，一个自动分拣照片的设备是非常必要的。</p>
<p>众所周知，近年关于图像分类的技术已经非常成熟，我们可以<del>易如反掌的</del>做一个自动分类机出来，但是里面涉及若干技术细节，了解这些细节既有助于各位同学<del>保持健康</del>了解一些神经网络的科普知识，又有助于大家学习一些Python的奇技淫巧。</p>
<h1 id="一-神经网络是啥">一、神经网络是啥</h1>
<p>之前老潘和一个同学针对神经网络进行过一些探讨，趁着这个时候回忆了一下当时我们探讨得出的一些看法。</p>
<p>众所周知，平时使用计算器的时候，需要在键盘上输入待计算的数字和算符，之后显示器会显示计算结果。</p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597824176011.png" alt="" loading="lazy"></figure>
<p>计算器算数的过程无非就是（1）：拿来要算的东西（2）：放进某种程序或者电路中（3）：把算完的结果再拿给你。这其中我们只能看到算前的式子和算后的结果。</p>
<p>那么神经网络运作类似，（1）：拿来要算的东西（2）：放进某种神经网络中（3）：把算完的结果再拿给你。这其中我们只能看到算前的东西和算后的结果。</p>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597824249108.png" alt="" loading="lazy"></figure>
<p>但是神经网络和普通计算器的区别在于，计算器输入只限数字和计算符号，输出一般就是一个数字或者其他化简后的算式。神经网络输入的东西形式多种多样，图片/声音/人脸/你的购买记录……输出的结果多种多样，例如算一下你的脸像猫的概率，算一下把你的声音变成老潘的声音是什么样的，算一下你双十一最可能买的东西……</p>
<p>计算器可以设计各种按钮接受一二三四五加减乘除号，神经网络也可以设计各种“类似按钮”接受图片声音购买记录。计算器用屏幕或者数码管显示计算完的结果，神经网络用列表显示这个输入的结果算完之后更像哪一个类别。那么具体这个网络怎么计算，怎么分类，分几类，就是各位<del>调参</del>工程师的工作了。</p>
<p>所以当时讨论的结果是，<strong>神经网络实质上是一个超大型的计算器</strong>。</p>
<p>有一个图片来描述人工智能，机器学习，深度学习，神经网络这些名词之间的关系：</p>
<p><img src="https://divertingPan.github.io/post-images/1597824306080.jpg" alt="" loading="lazy"><br>
人工智能，机器学习，神经网络，深度学习的关系：https://blog.csdn.net/lemonade_117/article/details/82668084</p>
<h1 id="二-怎么用神经网络计算">二、怎么用神经网络“计算”</h1>
<p>如果自己从头仿照主流的神经网络搭建并且用数据集训练，耗时巨大而且需要超强劲的设备。即使自己勉强训练出来，得到的结果一般也比不上Google等一票大团队使用超大数据集超强劲显卡超贵的电费训练好给大家用的预训练模型。直接拿这些模型来简单用一用已经足够，如果有特殊需求或者特殊的数据，还可以使用微调来做迁移学习。（名词：训练，科普解释为：通过设定好的一种算法，给一批题目和答案，让神经网络自己配置内部权重）</p>
<h2 id="21-resnet">2.1 ResNet</h2>
<p>这里使用ResNet50，加载ImageNet的预训练权重来试一下图片分类。（名词：权重，科普解释为：类比于计算器内部的接线和电路）</p>
<p>首先导入需要的库：</p>
<pre><code class="language-python">from keras.applications.resnet50 import ResNet50
from keras.preprocessing import image
from keras.applications.resnet50 import preprocess_input, decode_predictions
import numpy as np
</code></pre>
<p>加载模型，参数weights是加载预训练权重，否则使用随机化权重，include_top是指定是否包含顶部全连接层，全连接层最终输出这个图片属于每个类别下的概率，这个参数可能用于迁移学习。</p>
<pre><code class="language-python">model = ResNet50(weights='imagenet', include_top=True);
</code></pre>
<p>接下来把结果预处理一下，输进去，获取到结果：</p>
<pre><code class="language-python">img_path = 'test/4.jpg'; #加载图片
img = image.load_img(img_path, target_size=(224, 224)); #由于包含全连接层所以需要固定图片尺寸
x = image.img_to_array(img); #图片转成数组
x = np.expand_dims(x,axis=0); #增加一维
x = preprocess_input(x);
preds = model.predict(x);
</code></pre>
<p>通过matplotlib把结果统计并显示出来：</p>
<pre><code class="language-python">import matplotlib.pyplot as plt #加载绘图库

print('Path:',img_path, '\nPredicted:', decode_predictions(preds, top=8)[0]); #decode_predictions如果不指定top参数默认显示概率最高的前5个，指定几个显示几个
plt.figure(figsize=(8,8)); #默认plot图太小了
plt.subplot(2,1,1); #类似MATLAB画子图
plt.axis('off'); #关掉这里的坐标轴
plt.imshow(plt.imread(img_path)); #显示原图像
name_list = [decode_predictions(preds)[0][i][1] for i in range (0, 5)]; #关于列表推导式的使用见参考文献
num_list = [decode_predictions(preds)[0][i][2] for i in range (0, 5)]; #decode_predictions(preds)出的是一个嵌套结构的list
plt.subplot(2,1,2);
plt.barh(range(len(num_list)),num_list, tick_label = name_list); #把各类概率画个条状图
plt.show();
</code></pre>
<p>实际测试一下看看效果</p>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597824441196.PNG" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="5"><img src="https://divertingPan.github.io/post-images/1597824447464.PNG" alt="" loading="lazy"></figure>
<h2 id="22-vgg">2.2 VGG</h2>
<p>使用VGG16，基本步骤和上一节几乎一模一样，只是需要修改一下调用的库和加载的模型即可：</p>
<pre><code class="language-python">from keras.applications.vgg16 import VGG16
from keras.applications.vgg16 import preprocess_input, decode_predictions
 
model = VGG16(weights='imagenet', include_top=True)
</code></pre>
<p>实际测试效果与ResNet差别在于最高概率之外的识别结果稍有不同，但是对主体都能比较清晰的分出来。至于表情包水图，结果更杂乱了。有一个想法是，可以用两个网络对结果分两次类，即使真的有水图被误分进目标类别，也能够再被检出来？这个留作课堂作业，下课交给各班学委。</p>
<figure data-type="image" tabindex="6"><img src="https://divertingPan.github.io/post-images/1597824482922.PNG" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="7"><img src="https://divertingPan.github.io/post-images/1597824488434.PNG" alt="" loading="lazy"></figure>
<p>（ImageNet全1000分类见参考文献）</p>
<p>这里有一个坑是，直接在代码里调网络模型，下载速度可能会很慢（尤其VGG实在是太大了），所以可以手动下载好然后放在存储目录中：Linux路径~/.keras/models，Win路径C:\Users\老潘.keras\models，下载链接在参考文献中。</p>
<h1 id="三-自动分类">三、自动分类</h1>
<p>到这里其实就已经成送分题了，直接把对应图片按照最高的类别概率放进不同的文件夹就行了。重点是关注一下准确度，由于没有标注好的数据直接做测试集，所以就自己用眼睛看来判断吧。</p>
<p>Python实现拷贝文件操作，将img_path指定的文件移动到done文件夹下，命名为13.jpg：</p>
<pre><code class="language-python">import shutil
new_path = 'done/13.jpg';
path = shutil.copy(img_path, new_path)；
</code></pre>
<p>用Python遍历目录下的文件：</p>
<pre><code class="language-python">import os
 
result = []; #所有的文件的路径
for maindir, subdir, file_name_list in os.walk('test/'):
    print(&quot;1:&quot;, maindir); #当前目录
    print(&quot;2:&quot;, subdir); #当前目录下的所有目录
    print(&quot;3:&quot;, file_name_list);  #当前目录下的所有文件名
    for filename in file_name_list:
        apath = os.path.join(maindir, filename);#合并成一个完整路径
        result.append(apath);
</code></pre>
<p>到这里基本所有需要的模块都准备完毕了，交给实习生去整合一下代码就可以使用了。</p>
<pre><code class="language-python">#-*- coding: utf-8 -*-
from keras.applications.resnet50 import ResNet50
from keras.preprocessing import image
from keras.applications.resnet50 import preprocess_input, decode_predictions
import numpy as np
import matplotlib.pyplot as plt
import os
import shutil
 
model = ResNet50(weights='imagenet', include_top=True);
testing_dir = '火车/'; #存放待分类图片的路径
done_dir = 'done/'; #分类完毕的图片存放路径
temp_dir = 'temp/'; #未进入指定分类的图片，以免漏选
target_list = ['steam_locomotive', 'passenger_car', 'freight_car', 'electric_locomotive','bullet_train']; #指定挑出来的分类名称
 
for maindir, subdir, file_name_list in os.walk(testing_dir):
    print(&quot;testing directory:&quot;, maindir); #当前目录
    print(&quot;file list:&quot;, file_name_list);  #当前目录下的所有文件名
 
for file in file_name_list:
    img_path = maindir + file;
    img = image.load_img(img_path, target_size=(224, 224));
    x = image.img_to_array(img);
    x = np.expand_dims(x, axis=0);
    x = preprocess_input(x);
    preds = model.predict(x);
    if decode_predictions(preds)[0][0][1] in target_list:
        new_path = done_dir + file;
        path = shutil.copy(img_path, new_path);
        print('select: ' + path);
    else:
        new_path = temp_dir + file;
        path = shutil.copy(img_path, new_path);
        print('out: ' + path);
</code></pre>
<p>准确度不是特别高，因为老潘发现这个图片竟然被分类成了货车，图片里明明是空铁道</p>
<figure data-type="image" tabindex="8"><img src="https://divertingPan.github.io/post-images/1597824646827.PNG" alt="" loading="lazy"></figure>
<p>表情包和灌水图倒是都剔出去了，不过也有一两个由于特征确实不明显于是被遗漏没有被识别的：</p>
<figure data-type="image" tabindex="9"><img src="https://divertingPan.github.io/post-images/1597824661237.PNG" alt="" loading="lazy"></figure>
<h1 id="四-校花识别">四、校花识别</h1>
<p>之所以这次真的讲了校花识别，是因为<del>老潘良心发现不能当标题党</del>ImageNet对识别人脸来说是无力的，上面那种方法只限于识别除人脸之外的物品，想分出人类/身体/美女的脸是不行的。所以针对识别照片中有没有人的需求，要用别的方法来操作。</p>
<p>Face++上面有现成的API可以调用，注册一个账号可以获得使用一个免费试用的API接口，在官网上有很多应用场景，打开页面就可以直接体验一下，感觉效果好的话就可以用API接口在自己程序里直接调用，例如下面的在线测颜值打分：</p>
<figure data-type="image" tabindex="10"><img src="https://divertingPan.github.io/post-images/1597824694904.PNG" alt="" loading="lazy"></figure>
<p>关于API的应用，老潘在<a href="https://divertingpan.github.io/post/auto_lottery/">猎奇 | Python+ADB实现自动点赞和抽奖检测</a>里面用到了百度的文本识别。</p>
<p>在Python中调用Face++的API方法如下：</p>
<pre><code class="language-python">import requests
from json import JSONDecoder
from PIL import Image
 
#HumanBodyDetect API（V1）
http_url = &quot;https://api-cn.faceplusplus.com/humanbodypp/v1/detect&quot;
file_path = &quot;test/901.jpg&quot;
temp_path = &quot;temp/&quot;
data = {&quot;api_key&quot;: &quot;老潘家的潘老师&quot;,
        &quot;api_secret&quot;: &quot;Diverting_Pan&quot;,
        &quot;return_attributes&quot;: &quot;gender&quot;}
 
img = Image.open(file_path).convert('RGB').resize((512, 512), Image.BILINEAR) #API要求图片&lt;2M或&lt;1024*1024，而且色彩模式需要RGB
img.save(temp_path + &quot;human_temp.jpg&quot;) #压缩后的图片缓存一下
files = {&quot;image_file&quot;: open(temp_path + &quot;human_temp.jpg&quot;, &quot;rb&quot;)} #以二进制读入图像
response = requests.post(http_url, data=data, files=files) #POST上传
</code></pre>
<p>data里各个参数的作用可以在https://console.faceplusplus.com.cn/documents/4888383找到。这个API会返回图片中人物的性别的概率，所以可以根据有无此参数和此参数的值来筛选需要的图片。</p>
<p>若识别成功照片中有人，则将这个人的范围坐标、性别、穿衣颜色等信息包含在返回的信息里，如果没识别到有人则没有这些信息。通过读取返回的值，提取关键字段筛选有用的照片，以挑出有女性的照片为例：</p>
<pre><code class="language-python">req_con = response.content.decode('utf-8')
req_dict = JSONDecoder().decode(req_con) #解码成字典格式
if len(req_dict['humanbodies']): #如果图片中没有识别到人体则['attributes']['gender']['female']都不存在
    ifreq_dict['humanbodies'][0]['attributes']['gender']['female'] &gt; req_dict['humanbodies'][0]['attributes']['gender']['male']:
        print(&quot;Female&quot;)
    else:
        print(&quot;Not Female&quot;)
else:
    print(&quot;Not Female&quot;)
</code></pre>
<p>基本部件都准备完毕了，实习生可以再来加一下班了：（完整代码<a href="https://github.com/divertingPan/Get_Beauty_Image">见GitHub</a>）</p>
<pre><code class="language-python">#-*- coding: utf-8 -*-
import requests
from json import JSONDecoder
from PIL import Image
import os
import shutil
import time
 
http_url = &quot;https://api-cn.faceplusplus.com/humanbodypp/v1/detect&quot;
data = {&quot;api_key&quot;: &quot;_xdd7OMPd5rsHSsqwNaFTi-j-U_4T2HG&quot;,
        &quot;api_secret&quot;: &quot;fnGuZLo26p2vn0MWh6MCRgIdg5p_sESp&quot;,
        &quot;return_attributes&quot;: &quot;gender&quot;}
#HumanBodyDetect API（V1）
 
testing_dir = '校花/';
done_path = 'done/';
temp_path = 'temp/';
 
for maindir, subdir, file_name_list in os.walk(testing_dir):
    print(&quot;testing directory:&quot;, maindir); #当前目录
    print(&quot;file list:&quot;, file_name_list);  #当前目录下的所有文件名
 
for file in file_name_list:
    file_path = maindir + file;
    img = Image.open(file_path).convert('RGB').resize((512,512), Image.BILINEAR)
    img.save(temp_path + &quot;human_temp.jpg&quot;)
    files = {&quot;image_file&quot;:open(temp_path + &quot;human_temp.jpg&quot;, &quot;rb&quot;)}
 
    response = requests.post(http_url,data=data, files=files)
    req_con = response.content.decode('utf-8')
    req_dict = JSONDecoder().decode(req_con)
 
    if len(req_dict['humanbodies']):
        if req_dict['humanbodies'][0]['attributes']['gender']['female'] &gt;req_dict['humanbodies'][0]['attributes']['gender']['male']:
            new_path = done_path + file;
            path = shutil.copy(file_path, new_path);
            print('select: ' + path);
        else:
            new_path = temp_path + file;
            path = shutil.copy(file_path, new_path);
            print('out: ' + path);
    else:
        new_path = temp_path + file;
        path = shutil.copy(file_path, new_path);
        print('out: ' + path);
 
    time.sleep(1) #如果循环速度太快会被服务器断掉链接
</code></pre>
<p>有一个改进策略是记录下最近一次成功操作的文件，如果发现网络连接发生错误或者被服务器断掉连接就自动断点续传，&lt;del不过老潘懒得做了</del>可以留作课后作业交给各位想试着做的同学。</p>
<p>分类结果准确度不是特别高，不过也分出了大部分的水图，个别图片由于人体特征不明显或者性别特征不明显导致了分类失败</p>
<figure data-type="image" tabindex="11"><img src="https://divertingPan.github.io/post-images/1597824850811.PNG" alt="" loading="lazy"></figure>
<h1 id="五-总结">五、总结</h1>
<p>统计一下本次实验分类的情况</p>
<p>火车图片（ResNet50）总数 589</p>
<figure data-type="image" tabindex="12"><img src="https://divertingPan.github.io/post-images/1597824870269.png" alt="" loading="lazy"></figure>
<p>美女图片（Face++）总数 573</p>
<figure data-type="image" tabindex="13"><img src="https://divertingPan.github.io/post-images/1597824886513.png" alt="" loading="lazy"></figure>
<p>由于都是未加工的从网上抓的原生图片，所以只能依靠自己手动分类。而且很多图确实含混不清，比如在图片上只有一个火车车门，我把这类图也归结为有火车的类别了，依此标准来进行准确度的判别。不过从这个准确度看来，本次项目的两类自动分拣机，都有很大**调校（jiao）**的潜力。</p>
<p>从网上通过爬虫获取图片有一定几率导致图片结尾异常，从而导致程序读取图片出错</p>
<pre><code>（OSError: image file is truncated (0 bytes not processed)）
</code></pre>
<p>若遇到这种错误，删掉出错图像即可。可以添加一个检测图片读取成功的代码，不成功读取直接跳过（后记：可以用try/except）。<del>老潘懒得做了</del>留给大家作为课后作业。</p>
<h1 id="六-参考文献">六、参考文献</h1>
<p>[1] keras中使用预训练模型进行图片分类：https://www.cnblogs.com/vactor/p/9813108.html</p>
<p>[2] Keras中模型 《th与tf的区别》、《notop的含义》：https://blog.csdn.net/elvirangel/article/details/88854612</p>
<p>[3] keras的基本用法(四)——Fine Tuning神经网络：https://blog.csdn.net/quincuntial/article/details/72896690</p>
<p>[4] 全连接层有何作用？https://www.cnblogs.com/Terrypython/p/11147665.html</p>
<p>[5] 实战 迁移学习 VGG19、ResNet50、InceptionV3 实践 猫狗大战 问题：https://blog.csdn.net/pengdali/article/details/79050662</p>
<p>[6] keras离线下载模型的存储位置：https://www.jianshu.com/p/0a4ba12df520</p>
<p>[7] keras提供的所有模型下载地址：https://github.com/fchollet/deep-learning-models/releases</p>
<p>[8] 使用keras加载vgg16等模型权重文件失败的解决办法和模型.h5文件网盘下载地址：https://blog.csdn.net/icurious/article/details/80077035</p>
<p>[9] VGG16学习笔记：https://blog.csdn.net/dta0502/article/details/79654931</p>
<p>[10] VGG16学习笔记：https://www.cnblogs.com/lfri/p/10493408.html</p>
<p>[11] ResNet解析：https://blog.csdn.net/lanran2/article/details/79057994</p>
<p>[12] Python 列表推导式：https://www.runoob.com/note/15802</p>
<p>[13] Python——使用matplotlib绘制柱状图：https://www.cnblogs.com/decode1234/p/8535638.html</p>
<p>[14] ImageNet图像库1000个类别名称（中文注释不断更新）：https://blog.csdn.net/weixin_41770169/article/details/80482942</p>
<p>[15] python3之shutil高级文件操作：https://www.cnblogs.com/zhangxinqi/p/8038479.html</p>
<p>[16] Python遍历指定目录下的所有文件以及文件的过滤：https://blog.csdn.net/douyaoxin_126/article/details/88638359</p>
<p>[17]人工智能，机器学习，神经网络，深度学习的关系：https://blog.csdn.net/lemonade_117/article/details/82668084</p>
<p>[18] PYTHON 3 调用face++API代码（简单易懂）：https://blog.csdn.net/qq_41122796/article/details/79945873</p>
<p>[19]文档中心/HumanBody Detect API（V1）：https://console.faceplusplus.com.cn/documents/10071565</p>
<p>[20] Python3 字典：https://www.runoob.com/python3/python3-dictionary.html</p>
<p>[21]利用python PIL库进行图像模式的转换：https://www.jianshu.com/p/2e9539bdc307</p>
<p>[22] Python程序中PIL Image &quot;image file is truncated&quot;问题分析与解决：https://blog.csdn.net/scool_winter/article/details/89426509</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[进阶 | 利用Python开挂式获取网络资源——以搜集贴吧图片为例]]></title>
        <id>https://divertingPan.github.io/post/python_tieba/</id>
        <link href="https://divertingPan.github.io/post/python_tieba/">
        </link>
        <updated>2019-05-30T07:11:08.000Z</updated>
        <content type="html"><![CDATA[<p>这算是系列作了吗？反正今天主要内容仍旧是围绕利用互联网获取信息。这次内容是讲解Python的起家功能：爬虫，用百度贴吧的众多图片贴来入门爬虫思想和测试爬虫效果<del>（怎么和百度干上了？）</del></p>
<p>至于为什么叫爬虫不叫爬狗爬猫，这个来源已不可考。可能是这种行为很像一个虫子满屋到处无死角爬？</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597821106367.webp" alt="" loading="lazy"></figure>
<h1 id="摘-要">摘  要</h1>
<p>本项目以获取3000+来自百度贴吧的图片为目标，寓教于乐，注重实践，通过实际案例和详细步骤操作，揭露了挂逼在网上获取大量美女照片<del>骗取宅男钱财</del>的幕后操作。</p>
<h1 id="绪-论">绪  论</h1>
<p>如果你在2019年5月12日晚上得知，百度贴吧药丸，你混迹的贴吧里的几百个精华帖的几千副世界水准摄影作品马上要灰飞烟灭。你打算抢救，把这些图都下载到你的硬盘里。显然，一个一个的右键另存为是不可行的，多找几个人也没人愿意鸟你。怎么办呢？</p>
<p>这时我们的老朋友Python登场，带着他的好兄弟美容养颜毒鸡汤（BeautifulSoup），可以帮助你一边听音乐一边打游戏顺便吃个外卖，随手完成这个轻松<del>？</del>的工作。</p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597821282643.webp" alt="" loading="lazy"></figure>
<p>BeautifulSoup是Python的一个库，专职网络爬虫。爬虫的主要用途就是收集网络里大量、重复、繁杂的数据。比如说，贴吧里几千副世界水准的摄影作品，虽然数量很多，但是都集中在贴吧，而且也有一些规律性的指标帮助我们定位到图片的下载链接上，这就为实现下载图片自动化提供了契机。找规律的方法将在下文阐述。</p>
<p>接下来就是捡肥皂表演时间了，本篇所用程序基于CSDN博主‘Python编程爬虫工程师’的文章《这是一个Python百度爬虫，采集贴吧大佬们发布的所有美女照片》改编且详细分析讲解。请各位同学<del>注意力从美女照片上移开</del>认真听讲。</p>
<h1 id="一-让老朋友开始工作">一、让老朋友开始工作</h1>
<p>BeautifulSoup（下文简称BS）可以像浏览器那样，打开页面，下载文件，只是不通过鼠标点击，不通过屏幕显示画面，所有的动作统统通过代码做完。有点像之前讲的ADB。</p>
<p>BS可以接收一个URL链接（网址链接），并且解析链接背后的内容。如果把他接收的内容输出成文本，就能看见HTML代码。</p>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597821369678.PNG" alt="" loading="lazy"></figure>
<p>我们后面获取图片下载链接，需要依赖这个功能。而需要BS打开哪些链接，也需要事先做好调查，写好链接传送给BS。好在这些链接都有一些规律可循。下面来讲一讲如何找规律。</p>
<h1 id="二-找规律">二、找规律</h1>
<h2 id="21-图片链接的规律">2.1 图片链接的规律</h2>
<p>下载图片的过程很简单，拿到一个图片的下载链接，放进下载器，或交给BS就能存到你的电脑。获取下载链接有两种方法。<del>一是右键，选择另存为，这属于浏览器直接捕获链接并且执行下载。但是这种方法以低效且费鼠标而为人所唾弃。</del>二是在网页的HTML代码中找到图片的链接，收集很多这样的链接之后批量下载。如果你使用Google，在图片上直接<strong>右键</strong>，并且点击“<strong>检查</strong>”，你就可以<strong>打开装逼界面</strong>。</p>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597821436559.PNG" alt="" loading="lazy"></figure>
<p>注意，在右边的代码里，有一行被标记的代码，那就是刚刚检查的图片链接。如果直接点击那个链接，就可以单独打开这个图片，也可以放进下载器下载。还会看到这页其他图片的链接，指着他，对应的图片也会被标记，指着链接，图片的缩略图也能显示。</p>
<figure data-type="image" tabindex="5"><img src="https://divertingPan.github.io/post-images/1597821452994.png" alt="" loading="lazy"></figure>
<p>这时候观察一下代码，可以看到图片的下载链接都放在<code>&lt;img&gt;</code>的HTML标签里，这个标签都有一个<code>class</code>值<code>BDE_Image</code>，而且链接的属性名都是<code>src</code>。</p>
<figure data-type="image" tabindex="6"><img src="https://divertingPan.github.io/post-images/1597821568817.PNG" alt="" loading="lazy"></figure>
<p>所以，任务一下子简单了起来，如果能搞一个<code>自动开网页，自动找到“BDE_Image”的&lt;img&gt;标签，把这里的src给下载器</code>。就完成了图片的存储工作，根本不需要点鼠标。</p>
<pre><code class="language-python">
res = session.get(each)
soup = BeautifulSoup(res.text, 'html.parser') #关于打开网页，下文会讲
img_urls = soup.find_all('img', class_='BDE_Image') #这里会以列表结构存下所有的img标签内容
for img in img_urls:
    thumbnail = img.attrs['src'] #循环依次抽取图片链接
</code></pre>
<h2 id="22-开网页">2.2 开网页</h2>
<p>上面已经找到了图片的存放规律。现在问题就上升到如何打开这些网页？潘老师这么懒，肯定不会手动打开这些网页交给Python去弄。实际上，网页也是有分布规律的。</p>
<h3 id="221-贴吧操作">2.2.1 贴吧操作</h3>
<p>以贴吧为例，每个贴吧虽然名字都不同，但是网站却有很强规律性，一个贴吧链接示例（还有一些其他的参数但是对爬取工作并没有作用）：</p>
<p>https://tieba.baidu.com/f?kw=火车&amp;pn=0</p>
<p>显然，贴吧名是<code>火车</code>，<code>pn</code>是页码，<code>每翻一页，页码加50</code>，原因是一页显示50条帖子。找到了规律，就可以交给程序自动操作了。</p>
<pre><code class="language-python">tiezi_url = &quot;http://tieba.baidu.com/f?kw=&quot; + keyword + &quot;&amp;pn=&quot; + str(i * 50)
</code></pre>
<p>不过，我想要精品贴里面的图，其他垃圾帖子里可能很多都是广告或者水楼的表情包，点进精品贴，看到链接会发生变化：</p>
<p>https://tieba.baidu.com/f?kw=火车&amp;tab=good&amp;pn=0</p>
<p>只是在参数里多了一个<code>&amp;tab=good</code>的参数，那就对应修改上面代码多一个参数即可：</p>
<pre><code class="language-python">tiezi_url = &quot;http://tieba.baidu.com/f?kw=&quot; + keyword + &quot;&amp;tab=good&amp;pn=&quot; + str(i * 50)
</code></pre>
<p>贴吧里的帖子一共有几页？pn后面这个数要到几才能停下来？</p>
<p>因为刚刚也说了，贴吧一页显示50条帖子，所以我们使用<code>总帖子数÷50</code>，得到的数<code>向上取整</code>，这个数就是最后一页的页码了，使用<code>(页码-1)*50</code>（因为起始pn值是0），得到的就是<code>pn的最终数字</code>。于是新的问题是，如何获取帖子数，<del>如果有同学对如何向上取整有疑问请放学罚抄《Python入门到放弃》一百遍。</del></p>
<p>观察一下贴吧，潘老师在页面角落发现了<code>共有精品数2075个</code>字样：</p>
<figure data-type="image" tabindex="7"><img src="https://divertingPan.github.io/post-images/1597821787825.PNG" alt="" loading="lazy"></figure>
<p>对数字“检查”，于是发现这个数字在<code>class=&quot;red_text&quot;</code>的<code>&lt;span&gt;</code>标签下，由于这个文字是直接在文本区域，不属于任何一个属性，所以就直接利用<code>find().text</code>获得文本。获得这个数字之后做除法即可。</p>
<figure data-type="image" tabindex="8"><img src="https://divertingPan.github.io/post-images/1597821832070.PNG" alt="" loading="lazy"></figure>
<pre><code class="language-python">total_num = soup.find('span', class_='red_text').text
total_num = int(total_num.replace(',', '')) #原程序识别总帖子数时，需要删掉数中间的逗号分隔符
page_size = 50
total_page = math.ceil(total_num / page_size)
for i in range(0, total_page): 
    #这样会抓取所有的精品贴，如果嫌多可以改total_page为小一点的数字
    #另外，Python的for循环最后一次是range里的上限值-1
    tiezi_url = &quot;http://tieba.baidu.com/f?kw=&quot; + keyword + &quot;&amp;tab=good&amp;cid=0&amp;pn=&quot; + str(i * 50)
</code></pre>
<p>不要着急，潘老师应该测试一下最后一页的pn是不是=(向上取整(帖子数/50)-1)*50，免得打脸。</p>
<figure data-type="image" tabindex="9"><img src="https://divertingPan.github.io/post-images/1597821874944.PNG" alt="" loading="lazy"></figure>
<p>大呼三声：<code>百度老狗！</code>以解锁下文。</p>
<h3 id="222-帖子操作">2.2.2 帖子操作</h3>
<p>分析完贴吧的链接规律，再看看每个帖子的链接的规律。这是一个帖子的链接示例：</p>
<p>https://tieba.baidu.com/p/6112699622</p>
<p>非常直接了，不同帖子就是通过<code>最后那串数</code>区分开的。但是这不像贴吧名，这数是没什么实际意义的，怎么获取呢？</p>
<p>这就得<code>分析HTML代码</code>，找到每条帖子对应的这串数字。和找图片链接类似，在某条帖子的标题上，点击<code>右键</code>，<code>检查</code>，一下子就出来了。</p>
<figure data-type="image" tabindex="10"><img src="https://divertingPan.github.io/post-images/1597821947568.png" alt="" loading="lazy"></figure>
<p>提取的过程也类似，可以看到这串字符在<code>class=&quot;j_th_tit&quot;</code>的<code>&lt;a&gt;</code>标签中，属性名为href，把这串抽出来，接在http://tieba.baidu.com后面，就能获得一个帖子的真实有效链接了。</p>
<pre><code class="language-python">static_tiezi_urls.append(&quot;http://tieba.baidu.com&quot; + each.attrs['href'])
</code></pre>
<p>但是还有一点问题。帖子是<code>会翻页</code>的。翻页会引起链接怎样的变化呢？观察下面的这个帖子在第二页时的链接：</p>
<p>https://tieba.baidu.com/p/6112699622?pn=2</p>
<p>在最后多了一个<code>?pn=2</code>的参数。其实现在你也应该大致了解HTML的套路了。用一些不同的参数值来区分相似页面的不同状态，所以说网络爬虫是非常高效的。</p>
<p>于是只需要打开一个帖子之后，在链接后面加上这个参数，并且循环<code>pn=x</code>后面的这个数字，一直到最后一页，浏览完整个帖子。</p>
<p>怎么判断有没有到最后页呢？潘老师的做法是，检测页面有没有<code>下一页</code>的按钮，没有这个按钮，必定后面没有页了，就该结束了。检测按钮又需要分析HTML代码。在一个有“下一页”按钮的帖子里，“检查”这个按钮，</p>
<figure data-type="image" tabindex="11"><img src="https://divertingPan.github.io/post-images/1597822141170.PNG" alt="" loading="lazy"></figure>
<p>哎，不好搞哎，这个下一页不像刚才那个帖子数，不在标签里，也不是什么属性，重要的是这标签也没有class，BS的find()怕是find不到。不过可以直接对HTML代码本身下手。直接<code>检测页面代码</code>里有没有“下一页”三个字不太稳妥，因为HTML正文的文本里可能也会有这仨字。不过，如果检测页面有没有<code>&gt;下一页&lt;/a&gt;</code>这一串，应该就能稳一些了吧。</p>
<pre><code class="language-python">while(true):
    if '&gt;下一页&lt;/a&gt;' in res.text:
        page += 1
        each = re.sub(r'\?pn(.*)?$', '', each)
        each = each + '?pn=' + str(page)
    else:
        break
</code></pre>
<p>用了正则，因为上一页的链接尾部有<code>?pn=2</code>这样的页码，得删了旧的页码再写新页码。使用正则直接删除<code>从链接里唯一的问号开始，到链接结束</code>这中间的字符，之后再补上新的<code>pn=x</code>即可。</p>
<h1 id="三-下载图片">三、下载图片</h1>
<p>潘老师使用这个博主的代码下载图片的时候发现了以下问题：<strong>原代码的从贴吧获取帖子链接的部分class_='j_th_tit '后面这个空格是不可以留的</strong>，因为原本的class名也不可能会出现空格，即使HTML代码里类名后面有个空格，那空格也不会安在class的名字后面，在Python代码里给这个属性加了空格去识别反而会导致识别不到。</p>
<p>上面那是个小问题<del>因此程序不通导致潘老师调试10分钟算小问题吗</del>，第二是帖子翻页的问题，<strong>按照原程序，只能打开帖子的第一页下载图片</strong>。<del>固然美女贴通常只有在一楼有照骗楼下基本都是舔狗，</del>我们需要追求程序的通用性，所以上一节的翻页爬虫是必需的。</p>
<p>潘老师还临时发现一个问题是帖子数量问题。原程序获取的是帖子数，帖子其实是俗称的“楼”，实际贴吧的文章量应该是主题数，主题数在贴吧的左下角，要捉这个数字才对。只能怪百度老狗自己都没搞清楚这乱七八糟的数字该怎么管理。</p>
<p>再一个是图片尺寸的问题。<strong>原贴直接从帖子页面获取图片链接，但是直接打开帖子，不管是显示还是下载，得到的都是压缩过的小图</strong>。必须点开图片才能获得高清大图。本着<del>看美女就要</del>看高清图的思想，潘老师决定拓展一下原程序的功能。让老朋友加加班，帮我们点进大图去下载。</p>
<p>例如打开一个图片帖子，不管你右键存图也好，获取下载链接存图也好，图就只有这么大：</p>
<figure data-type="image" tabindex="12"><img src="https://divertingPan.github.io/post-images/1597822403913.PNG" alt="" loading="lazy"></figure>
<p>但点击一个图片之后，<code>进入看图模式</code>，存这个图就是高清大图惹：</p>
<figure data-type="image" tabindex="13"><img src="https://divertingPan.github.io/post-images/1597822424976.PNG" alt="" loading="lazy"></figure>
<p>思路很简单，照搬前面的做法基本就够用。由于没法从帖子直接获得看图模式的链接，所以就得观察这个页面的链接看看有什么规律。下面是两个不同帖子的不同图片的链接：</p>
<p>http://tieba.baidu.com/photo/p?kw=火车&amp;flux=1&amp;tid=6112699622&amp;pic_id=00815aafa40f4bfba1fea20f0d4f78f0f63618c5&amp;pn=1&amp;fp=2&amp;see_lz=1&amp;red_tag=p1247750067</p>
<p>http://tieba.baidu.com/photo/p?kw=火车&amp;flux=1&amp;tid=6106875508&amp;pic_id=f08d0cf3d7ca7bcb4276997eb0096b63f724a8d1&amp;pn=1&amp;fp=2&amp;see_lz=1&amp;red_tag=y1523450118</p>
<p>这个难度稍微大了点，毕竟到压轴题了啊。不过不慌。首先经过试验，<code>&amp;red_tag=</code>不会影响对图片的加载，删除之。不同帖子，不同图片，都有一样的部分（标红）：</p>
<p><code>http://tieba.baidu.com/photo/p?kw=火车&amp;flux=1</code>&amp;tid=6106875508&amp;pic_id=f08d0cf3d7ca7bcb4276997eb0096b63f724a8d1<code>&amp;pn=1&amp;fp=2&amp;see_lz=1</code></p>
<p>不同的只有<code>&amp;tid=</code>和<code>&amp;pic_id=</code>，这就非常简单了，tid就是这个帖子地址后面的那串数字，pic_id是在帖子里的小图片的下载链接的图片文件名。对比二者链接即可发现对应关系：</p>
<figure data-type="image" tabindex="14"><img src="https://divertingPan.github.io/post-images/1597822515684.PNG" alt="" loading="lazy"></figure>
<p>好的，进入大图页面后，把大图存下来即可。这里操作和2.1一致，在大图上右键检查，<code>&lt;img&gt;</code>标签下，<code>class=&quot;image_original_original&quot;</code>，<code>src</code>后面即为图片真实可用的链接了。</p>
<figure data-type="image" tabindex="15"><img src="https://divertingPan.github.io/post-images/1597822582300.PNG" alt="" loading="lazy"></figure>
<p>结果潘老师兴致勃勃以为这样可以获取美女高清大图的时候，百度老狗悄咪咪说</p>
<figure data-type="image" tabindex="16"><img src="https://divertingPan.github.io/post-images/1597822592706.webp" alt="" loading="lazy"></figure>
<p>这么做是不行的。</p>
<p>潘老师发现这么做根本下载不来图。调试的时候发现，程序压根就没有获取到src链接。经过仔细确认是不是代码不小心多了少了点字母之后，潘老师发现</p>
<figure data-type="image" tabindex="17"><img src="https://divertingPan.github.io/post-images/1597822613428.PNG" alt="" loading="lazy"></figure>
<p>怎么页面里没有这个标签吗？那图片的链接藏在哪了？</p>
<figure data-type="image" tabindex="18"><img src="https://divertingPan.github.io/post-images/1597822634507.PNG" alt="" loading="lazy"></figure>
<p>？？所以说用浏览器和用BS打开的页面不一样？？不过确认之后，这链接确实也是高清大图的链接。</p>
<figure data-type="image" tabindex="19"><img src="https://divertingPan.github.io/post-images/1597822667281.PNG" alt="" loading="lazy"></figure>
<p>所以说，直接点击帖子里的图片进入大图模式，图并没有加载在网页里，猜测可能是有一个类似控件的东西在承载着显示图的功能，而这个控件没法被BS加载。</p>
<p>所以直接把接到的<code>HTML转成文本</code>，利用正则<code>抽出图片URL</code>。</p>
<pre><code class="language-python">res_img = session.get(high_img) #利用session模拟登陆后，加载网页
soup_img = BeautifulSoup(res_img.text, 'html.parser') #获取网页内容，html.parser解析
s = str(soup_img)
r = re.findall(r'(?&lt;=&quot;waterurl&quot;:&quot;)(?![\w\W]*?&quot;waterurl&quot;:&quot;).*(?=&quot;},&quot;medium&quot;)', s)
high_img_urls = ''.join(r) #正则返回一个列表，需要将他展开成字符串
</code></pre>
<h1 id="四-最终演示">四、最终演示</h1>
<p>基本已经完工了，如果没有意外的话就可以交给实习程序员让他去完善代码了。原代码说不登陆没法正常用，不过潘老师试验并不影响。顺带一提，Python模拟登陆通过requests库的session完成。</p>
<figure data-type="image" tabindex="20"><img src="https://divertingPan.github.io/post-images/1597822785729.webp" alt="" loading="lazy"></figure>
<p>运行，输入贴吧名字，回车即可使用。</p>
<figure data-type="image" tabindex="21"><img src="https://divertingPan.github.io/post-images/1597822809589.PNG" alt="" loading="lazy"></figure>
<p>哎呀，手滑了呀，我是想输入“火车”，什么垃圾输入法。搜狗果然是比百度更老的狗。</p>
<figure data-type="image" tabindex="22"><img src="https://divertingPan.github.io/post-images/1597822826907.PNG" alt="" loading="lazy"></figure>
<p>嗯，来看看都抓了些什么图出来。</p>
<figure data-type="image" tabindex="23"><img src="https://divertingPan.github.io/post-images/1597822841525.PNG" alt="" loading="lazy"></figure>
<p>看来是不小心抓了一个大水贴啊。往后看看正经图片，看看清晰度如何。</p>
<figure data-type="image" tabindex="24"><img src="https://divertingPan.github.io/post-images/1597822854225.PNG" alt="" loading="lazy"></figure>
<p>不错，连EXIF信息都有。所以说啊各位同学，记得<code>关闭手机拍照带GPS</code>的功能，不然人家真的是顺着网线找到你。</p>
<figure data-type="image" tabindex="25"><img src="https://divertingPan.github.io/post-images/1597822882875.webp" alt="" loading="lazy"></figure>
<h1 id="五-常见报错解决指南">五、常见报错解决指南</h1>
<p>这种短时间内开发出来的程序，必定会有不成熟的地方。潘老师自己测试的时候发现了以下可能发生的错误，并附上解决方法。</p>
<h2 id="51-连接失败">5.1 连接失败</h2>
<p>如果使用时突然报错：</p>
<pre><code>ConnectionError: ('Connection aborted.', TimeoutError(10060, '由于连接方在一段时间后没有正确答复或连接的主机没有反应，连接尝试失败。', None, 10060, None))
</code></pre>
<p>这种情况多半<del>是百度发现你在干坏事封了你的号</del>是你的<code>网速不太好</code>，也许是因为室友不小心剪断了你的网线，或者在偷用热点下载什么见不得人的影片造成的。</p>
<p>如果使用时突然报错：</p>
<pre><code>ConnectionError: ('Connection aborted.', ConnectionAbortedError(10053, '你的主机中的软件中止了一个已建立的连接。', None, 10053, None))
</code></pre>
<p>这种情况是你的<code>网络状态不稳定</code>，一般是因为室友不小心剪断了你的网线造成的。</p>
<h2 id="52-图片下载错误">5.2 图片下载错误</h2>
<p>极特殊情况下会出现某个帖子里的图片下载下来后文件异常，一般该错误具有<code>帖子集中性</code>。经潘老师反复检测，图片链接，下载信道均无明显异常或干扰。猜测是由于百度老狗的<code>神秘力量</code>造成的。</p>
<figure data-type="image" tabindex="26"><img src="https://divertingPan.github.io/post-images/1597823016756.PNG" alt="" loading="lazy"></figure>
<p>综上，程序出错都是你自己或者百度的原因，和潘老师没有一点儿关系。</p>
<h1 id="参考文献">参考文献</h1>
<p>[1] 这是一个Python百度爬虫，采集贴吧大佬们发布的所有美女照片. https://blog.csdn.net/qq_41841569/article/details/83792663</p>
<p>[2] python str,list,tuple转换. https://www.cnblogs.com/nopnog/p/7066578.html</p>
<p>[3] 使用正则匹配最后一个字符串. https://blog.csdn.net/qq_18703313/article/details/78794364</p>
<p>[4] 正则表达式在线测试.https://c.runoob.com/front-end/854</p>
<p>[5] Python中BeautifulSoup库的用法. https://blog.csdn.net/qq_21933615/article/details/81171951</p>
<p>[6] 火车吧-百度贴吧. https://tieba.baidu.com/f?kw=%E7%81%AB%E8%BD%A6&amp;ie=utf-8&amp;tab=good</p>
<h1 id="附录1">附录1</h1>
<p>以下是潘老师进行整个实验所用的程序全文。报错缺module的话先pip安装对应的库。环境Python3.6。或者<a href="https://github.com/divertingPan/Get_Beauty_Image">前往GitHub下载完整项目</a>，非常神奇的是，这个项目已经送去北极的冰库了，老潘也不知道为什么。</p>
<pre><code class="language-python"># -*- coding: utf-8 -*-

import requests, math, os.path, re
from bs4 import BeautifulSoup
        
# 模拟登陆的方法
def login():
    # 登录的用户名
    username = '老潘家的潘老师'
    # 登录的密码
    password = 'divertingPan'
    # 登录所需的参数
    login_data = {
            'username': username,
            'password': password,
            'u': 'https://passport.baidu.com',
            'tpl': 'pp',
            'staticpage': 'https://passport.baidu.com/static/passpc-account/html/v3Jump.html',
            'isPhone': 'false',
            'charset': 'utf-8',
            'callback': 'parent.bd_pcbs_ra48vi'
            }
    # 构造所需的headers
    headers = {
            &quot;user-agent&quot;: &quot;Mozilla/5.0 (Windows NT 6.1; WOW64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/49.0.2623.112 Safari/537.36&quot;
            }
    # 登录的url
    baidu_loginurl = &quot;https://passport.baidu.com/v2/api/?login&quot;
    # 创建session
    session = requests.session()
    # 模拟登陆
    session.post(baidu_loginurl, login_data, headers=headers)
    # 创建session
    return session

# 获取总页数的方法
def get_total_page(url, session):
    # 获取网页内容
    res = session.get(url)
    # 解析出帖子总数
    soup = BeautifulSoup(res.text, 'html.parser')
    tiezi_total_num = soup.find('span', class_='red_text').text
    tiezi_total_num = int(tiezi_total_num.replace(',', ''))
    # 贴吧默认每页50条帖子
    page_size = 50
    # 计算出总页数
    total_page = math.ceil(tiezi_total_num / page_size)
    return total_page

# 获取帖子url列表的方法
def get_tiezi_url_list(total_page, url, session):
    #下面循环几次就是抓几页的帖子
    #是贴吧的翻页，不是帖子的翻页，帖子都会从一楼抓到最后楼
    all_tiezi_urls = []
    for i in range(0, 5):
        # 构造url
        tiezi_url = url + &quot;&amp;pn=&quot; + str(i * 50)
        # 获取每一页的帖子url
        tiezis = get_tiezi_list(tiezi_url, session)
        # 将所有帖子url加入到all_tiezi_urls中
        all_tiezi_urls.extend(tiezis)
    return all_tiezi_urls

# 获取当前页所有帖子列表的方法
def get_tiezi_list(tiezi_url, session):
    # 获取某个帖子列表页面的网页内容
    res = session.get(tiezi_url)
    # 从中解析出每个帖子的链接
    soup = BeautifulSoup(res.text, 'html.parser')
    a_list= soup.find_all('a', class_='j_th_tit') #???
    # 拼接出真实的帖子链接
    static_tiezi_urls = []
    for each in a_list:
        static_tiezi_urls.append(&quot;http://tieba.baidu.com&quot; + each.attrs['href'])
    return static_tiezi_urls

if __name__ == '__main__':
    session = login()
    keyword = input('请输入贴吧名称：')
    # 构建贴吧主页url
    index_url = 'https://tieba.baidu.com/f?kw={0}&amp;tab=good'.format(keyword)
    # 获取帖子总页数（经过测试发现 不登录并不影响）
    total_page = get_total_page(index_url, session)
    # 获取每一页的帖子url 放在列表中
    all_tiezi_urls = get_tiezi_url_list(total_page, index_url, session)
    #以“关键字/数字序号.jpg”的形式保存爬取的图片
    #判断文件夹是否存在 如果不存在 则先创建文件夹
    img_dir = keyword
    if (not os.path.exists(img_dir)):
        os.mkdir(img_dir)
    i = 0
    for each in all_tiezi_urls:
        # 获取帖子的具体内容
        page = 1
        while(True):
            print('进入一个贴，第' + str(page) + '页')
            res = session.get(each)
            # 从中解析出图片url
            soup = BeautifulSoup(res.text, 'html.parser')
            img_urls = soup.find_all('img', class_='BDE_Image')
            for img in img_urls:
                i += 1                
                thumbnail = img.attrs['src']
                pic_id = ''.join(re.findall(r'(?&lt;=/)(?![\w\W]*?/).*(?=.jpg)', thumbnail))
                tid = ''.join(list(re.findall(r'p/(.*)\?pn|p/(.*)$', each)[0]))
                print(&quot;正在下载第&quot; + str(i) + &quot;张图片&quot; )
                high_img = 'http://tieba.baidu.com/photo/p?kw=' + keyword + '&amp;flux=1&amp;tid=' + tid + '&amp;pic_id=' + pic_id
                res_img = session.get(high_img)
                soup_img = BeautifulSoup(res_img.text, 'html.parser')
                
                # baidu老狗又调皮了
                high_img_urls = ''.join(re.findall(r'(?&lt;=&quot;waterurl&quot;:&quot;)(?![\w\W]*?&quot;waterurl&quot;:&quot;).*(?=&quot;},&quot;medium&quot;)', str(soup_img)))
                with open(img_dir + &quot;/&quot; + str(i) + '.jpg', 'wb') as f:
                    f.write(session.get(high_img_urls).content)
            
            if '&gt;下一页&lt;/a&gt;' in res.text:
                page += 1
                each = re.sub(r'\?pn(.*)?$', '', each)
                each = each + '?pn=' + str(page)
            else:
                break
    print(&quot;图片下载完毕！&quot;)
</code></pre>
<h1 id="附录2">附录2</h1>
<p>伸手党同学可能会说，算了吧老潘，你看你刚才也手滑去抓校花了<del>什么糟糕台词？</del>，干脆你直接放一些好看的校花图给我们看看吧。既然今天已经拖堂这么久了，潘老师就奖励大家，特选出无名校花一二三，各位一起放松一下。</p>
<figure data-type="image" tabindex="27"><img src="https://divertingPan.github.io/post-images/1597823225269.webp" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="28"><img src="https://divertingPan.github.io/post-images/1597823252009.jpg" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="29"><img src="https://divertingPan.github.io/post-images/1597823258113.jpg" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="30"><img src="https://divertingPan.github.io/post-images/1597823296110.jpg" alt="" loading="lazy"></figure>
<p>现在开始评选第一届“Python杯”我心目中最美的校花奖项。请在评论区投出你庄严而珍贵的一票。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[续前文 | 百度贴吧清贴事件、屏蔽广告的解决方案]]></title>
        <id>https://divertingPan.github.io/post/ban_baidu_ads/</id>
        <link href="https://divertingPan.github.io/post/ban_baidu_ads/">
        </link>
        <updated>2019-05-29T15:58:19.000Z</updated>
        <content type="html"><![CDATA[<p><a href="https://divertingpan.github.io/post/google_using/">之前那篇</a>关于用百度的推文着重讲如何检索信息，而且面向初级用户。现在补充一些和百度老狗有关的东西，并不局限于信息检索。</p>
<h1 id="一-关于贴吧清2017年以前帖子的解决方案">一、关于贴吧清2017年以前帖子的解决方案。</h1>
<p>2019年5月13日，百度贴吧发了这么个公告：</p>
<blockquote>
<p>由于数据系统升级，程序猿gg们将2017年1月1日以前的贴子们都藏起来咯——所以可能会导致你遇到以下现象：<br>
1、【吧内贴子列表页】被隐藏的贴子，显示数据加载失败或加载中；<br>
2、【我的收藏】贴数量无故减少；<br>
3、【消息通知页】，17年1月1日以前的@及回复内容，无法进入查看原帖；<br>
遇到以上情况的吧友们请稍安勿躁——问题进展可随时关注意见反馈吧的相关公告，感谢大家的理解与支持——</p>
</blockquote>
<p>大量用户不禁<del>口出粗鄙之语，</del>纷纷逃离。潘老师也不禁开始考虑要把自己的推文多备几份。<br>
不过今天潘老师摸索出一个解决方法来暂时应对。首先在百度搜索你想看的已经没了的帖子标题，可以看到这篇帖子发帖时间是2017年前的，确实打不开了。这时只需<strong>点击“百度快照”</strong>：</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597766507343.webp" alt="" loading="lazy"></figure>
<p>就可以进入类似网页备份的东西里，页面加载出来了，可以看出最后一次的备份正是灾难发生前的那一刻。所以各位想看的精华帖攻略贴<del>同人（H）</del>文应该大概率是还可以打开一些的。</p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597766564855.webp" alt="" loading="lazy"></figure>
<p>但是这个做法有一个bug，如果你翻页的话，就不好用了，如何找到第二页，第三页，……，第n页的快照？一个想法是，搜索到帖子第二页的内容的关键词，用快照直接打开第二页。但问题在于谁能记得哪页有哪些内容呢？所以这个方法还有待完善。<br>
（图的加载也有问题，都是图的帖子可能就比较惨了）</p>
<h1 id="二-消除百度广告等乱七八糟反正就是广告的解决方案">二、消除百度广告等乱七八糟反正就是广告的解决方案。</h1>
<p>感谢@李朝阳 同学的提醒，潘老师忘记了万能的油猴（Tampermonkey），这个实在是太牛逼了，而且相关信息也很多，潘老师直接引用别人做好的调查工作即可：<br>
TamperMonkey | 一款过滤百度广告推广的神器：<br>
https://blog.csdn.net/qq_35976320/article/details/80985699<br>
这个帖子不仅给了<strong>消除广告</strong>的方法，还有<del>破解视频网站VIP视频的方法，百度网盘不限速的方法。牛逼。</del></p>
<p>实际测试一下百度消广告的功能，另外两种功能就悄悄自己尝试吧反正大家都懂。<br>
不开插件，这一屏都是广告：</p>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597766678898.webp" alt="" loading="lazy"></figure>
<p>开了插件之后，除了携程（牛逼？）没屏蔽，其他乱七八糟都被折叠了。妈妈再也不用担心我治疗脱发被莆田系医院骗了。</p>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597766698696.jfif" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[教程 | 如何利用互联网高效检索信息——以百度搜索为例]]></title>
        <id>https://divertingPan.github.io/post/google_using/</id>
        <link href="https://divertingPan.github.io/post/google_using/">
        </link>
        <updated>2019-05-28T15:19:47.000Z</updated>
        <content type="html"><![CDATA[<p>潘老师一度以为在简历里写“<strong>会熟练使用百度解决问题</strong>”是一个笑话。近期潘老师和身边的同学陆续提交毕业论文，这时候大家对写论文或者论文格式甚至Word怎么使用都充满了疑问，也是这时候潘老师才发现，原来高效使用百度确实是一个技术活。所以，今天就专门写一篇利用互联网资源解决问题的科普文，希望<del>不会科学上网去Google甚至根本没听说过Google的</del>同学们最大化利用百度带给人们的便捷功能。本篇不涉及什么专业知识，建议各位都<del>走马观花</del>阅读一下。</p>
<h1 id="摘-要">摘  要</h1>
<p>使用百度搜索资料也是当今时代所需的重要技能。本文从实际调查结果出发，通过结合实际案例，纠正了使用百度的误区，给出了利用百度高效检索信息的方法，列举了各类型信息的相关渠道。</p>
<p><strong>关键词</strong>：百度; 信息检索; 怎么办</p>
<h1 id="abstract">Abstract</h1>
<p>In the 21st century, it's an important skill that using Google to search for information. After various fvck words from classmates, the misusing of Google is found and corrected. Some efficient methods of using Google are given. Some webs with every type of information are enumerated.</p>
<p><strong>Keywords</strong>: Google; information retrieval; how to do</p>
<h1 id="引-言">引  言</h1>
<p>现在已经是21世纪了，连你做毕设写论文都可以轻易的从网上获取海量素材来完善你<del>那原本是一坨垃圾</del>的华丽项目。现在的年轻人基本有什么问题，第一个想到的并不是去问老师家长，而是去网上百度（例如困扰大家的“脱发怎么办”）。当然，比较时髦的同学可能会上Google搜索问题。但是，很多情况下，通过百度甚至是Google并不能直接得到我们想要的答案，更多时候都是广告广告广告广告广告（以至于你真的去了莆田系医院治疗脱发）。怎样得到真实有效的信息呢？百度搜索是不是真的那么不堪呢？</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597765271596.webp" alt="" loading="lazy"></figure>
<p>其实并不是，潘老师<del>因为穷逼没有很多科学上网流量</del>也总是用百度，近期也实际和一些同学交流或者亲自看他们如何使用百度，这时候才发现问题。很多同学使用方法有很大<strong>误区</strong>，知道一些<strong>进阶</strong>功能的同学就更少了，下面就这些问题一一展开讨论。</p>
<h1 id="如何在百度搜索问题以电脑死机为例">如何在百度搜索问题——以电脑死机为例</h1>
<p>假设你有一台心爱的价值28888的集游戏影音设计于一体的工作站，明天就要答辩了，你现在正熬夜做答辩的PPT，突然电脑死机了，并且再也开不开机。来不及打电话给潘老师来修电脑的你决定，自己动手搞好他。这时候最好的办法是拿出手机，到百度搜索解决办法。例如：</p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597765439640.PNG" alt="" loading="lazy"></figure>
<p>暂且不说提问句里这个极富感情力的**“啊”**根本就没有参与到检索中，而且大家也会发现，检索结果对你的状况根本没有一点帮助。你明明着急的是死机之后再也开不开机，但是结果里大多都是死机的时候你要怎么办。然而，这种提问方法，的确是潘老师所见大多数同学采用的方式。</p>
<p><strong>▶正确做法，关键词检索法：</strong></p>
<p>首先请你不要带感情色彩什么“啊”“呀”“啊啊啊！”这些统统<strong>不要出现</strong>在你的搜索框好吗？更重要的是，请着重在你<strong>最关键的问题</strong>上，找出能概括你问题的<strong>关键词</strong>，例如“台式机”“按开机键”“没反应”，并且用空格分开。这样，你的搜索结果就更贴合你的实际情况了：</p>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597765613602.PNG" alt="" loading="lazy"></figure>
<p>这时候根据列出的各条结果，可以看到，大多数人提到的是电源没插好、电源坏了、主板问题，甚至有的回答还说，能根据报警声进一步判断[1]：</p>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597765654935.PNG" alt="" loading="lazy"></figure>
<p>于是你依次检查是不是停电了？是不是插座松了？是不是开机按钮坏了？拆了室友的电源试试是不是电源坏了？（当然电源怎么接线就又需要百度了）</p>
<p>概括而言，关键词检索法的核心就是，<strong>在搜索框里输入能全面、准确形容问题的关键词</strong>。搜索电脑蓝屏时包含蓝屏上的错误代码，搜索旅游攻略时标明你的地点交通方式季节预算等等。</p>
<p><strong>▶进阶玩法，运算符法：</strong></p>
<p>百度搜索的关键词里可以使用一些符号，最常用的是“与或非”逻辑运算符，空格代表条件叠加，相当于“和”或者“与”；【|】（竖线）代表条件的并列，相当于“或”，【 -】（空格负号）代表排除后面这个条件，相当于“非”[2]。例如你想搜索关于“三体”的信息，却不想看到已经烂尾了的电影信息，你就可以搜索：</p>
<p><img src="https://divertingPan.github.io/post-images/1597765697507.PNG" alt="" loading="lazy"><br>
上图是直接搜索“三体”的结果</p>
<p><img src="https://divertingPan.github.io/post-images/1597765703520.PNG" alt="" loading="lazy"><br>
上图是搜索“三体 -电影”排除和电影有关信息的结果</p>
<h1 id="如何找到最合适的信息以搜索图片素材为例">如何找到最合适的信息——以搜索图片素材为例</h1>
<p>上一节解释了如何找到真正对自己有帮助的信息，其实很多同学是因为自己的提问方式不当才会觉得百度很笨，想要的都找不到，甚至还有一堆垃圾信息。那么进一步的，用简洁有效的关键词搜索到答案之后，如何在这么多链接中发现最靠谱的信息呢？例如你需要一个火箭图片作为你的答辩PPT素材，此时你在百度搜索：</p>
<figure data-type="image" tabindex="5"><img src="https://divertingPan.github.io/post-images/1597765782887.PNG" alt="" loading="lazy"></figure>
<p>很好，你已经学会了上面提到的关键词检索法，但是，潘老师在往期的一篇文章中提到过，搜索图片时，如果采用百度图片里这些图，不仅清晰度难以保证，而且前一阵关注度特别高的视觉中国案例也容易让大家栽坑。所以，具体信息需要具体网站才能胜任，毕竟术业有专攻。下面是一些关于常用信息的靠谱来源：</p>
<table>
<thead>
<tr>
<th style="text-align:center">信息类型</th>
<th style="text-align:center">可信度较高的来源</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">旅游、美食攻略</td>
<td style="text-align:center">知乎、微博</td>
</tr>
<tr>
<td style="text-align:center">电子、计算机、编程</td>
<td style="text-align:center">github、CSDN、博客园</td>
</tr>
<tr>
<td style="text-align:center">图片</td>
<td style="text-align:center">unsplash</td>
</tr>
<tr>
<td style="text-align:center">游戏攻略</td>
<td style="text-align:center">论坛</td>
</tr>
<tr>
<td style="text-align:center">疾病</td>
<td style="text-align:center">医院</td>
</tr>
<tr>
<td style="text-align:center">Photoshop教程</td>
<td style="text-align:center">老潘家的潘老师</td>
</tr>
</tbody>
</table>
<p>根据上表，可知，你这时候应该先百度搜索unsplash网站，再进入搜索火箭图片。或使用下节所述方法检索。</p>
<p><img src="https://divertingPan.github.io/post-images/1597765979386.png" alt="" loading="lazy"><br>
上图显示了在百度找到unsplash并且在该网站中找到火箭图片的过程，这甚至不需要科学上网</p>
<p>所以说，检索信息的时候不要总把目光限制在使用百度。各种带搜索框的渠道都可以搜索。最骚的操作是，利用<strong>淘宝搜索宝贝详情</strong>获取到某个元件的外表和详细参数[3]：</p>
<figure data-type="image" tabindex="6"><img src="https://divertingPan.github.io/post-images/1597766013151.png" alt="" loading="lazy"></figure>
<h1 id="如何找指定渠道下的信息以搜索旅游攻略为例">如何找指定渠道下的信息——以搜索旅游攻略为例</h1>
<p>上一节给出了找不同类型信息需要对应不同网站的思路。然而如何直接定位到指定网站下的相关信息？</p>
<p>假设你顺利通过答辩，想去放飞一把自我，找一下北京故宫的具体游览路线，想看看知乎上各位dalao的旅游经验。你可以点击搜索栏右下角的“<strong>搜索工具</strong>”：</p>
<figure data-type="image" tabindex="7"><img src="https://divertingPan.github.io/post-images/1597766036286.PNG" alt="" loading="lazy"></figure>
<p>在“<strong>站点内检索</strong>”里输入zhihu.com，再搜索：</p>
<figure data-type="image" tabindex="8"><img src="https://divertingPan.github.io/post-images/1597766053070.PNG" alt="" loading="lazy"></figure>
<p>除去头几条的广告信息（百度特色），你就会得到下面这些从知乎搜索来的页面了。<del>所以为什么不直接去知乎APP上搜索？因为本文是以百度为例啊使用知乎的搜索框搜索不就跑题了吗？</del></p>
<figure data-type="image" tabindex="9"><img src="https://divertingPan.github.io/post-images/1597766071427.PNG" alt="" loading="lazy"></figure>
<p>这一招对于某些不支持网站内搜索的网站，用处还是挺大的。</p>
<h1 id="总-结">总  结</h1>
<p>以上通过三个实际例子，对使用百度检索自己疑问的方法做了简单讨论。希望本文能抛砖引玉，帮助各位同学掌握互联网资源检索的高效方法，让互联网海量资源成为各位的知识宝库。<del>以后再有同学问潘老师能够百度解决的问题潘老师就要罚站这位同学了。但是如果你问潘老师这个问题该怎么百度，潘老师还能勉强原谅你一次。</del></p>
<h1 id="致-谢">致  谢</h1>
<p>感谢各位不会用百度、用不好百度的同学为潘老师提供了写作灵感。感谢百度提供的检索平台<del>让我利用百度找Google。</del></p>
<h1 id="参考文献">参考文献</h1>
<p>[1] 台式机按开机键后没反应怎么处理.http://ask.zol.com.cn/x/3393473.html.</p>
<p>[2] 百度的检索运算符.https://wenku.baidu.com/view/71bbcd6e4431b90d6d85c724.html.</p>
<p>[3] 潘老师.教你怎样修复坏冰箱[C].老潘家的潘老师,2018.</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[记录 | 一个Debug经历&&一个跳出双层循环的方法]]></title>
        <id>https://divertingPan.github.io/post/debug_exp/</id>
        <link href="https://divertingPan.github.io/post/debug_exp/">
        </link>
        <updated>2019-05-09T14:14:12.000Z</updated>
        <content type="html"><![CDATA[<p>首先声明：为了保持严谨的求学态度，各位程序员面对bug时还是尽量搞懂问题的根源，否则后期这个bug可能会造成很大的影响（其实很多情况下也不会再造成什么影响）。</p>
<p>第二条声明：这篇记录只是写了一个解决问题的路子，里面具体技术细节老潘也没太搞懂，也没有必要搞懂，反正快速得到需要的结果就够了。所以有问题不要问老潘，老潘也不懂，请百度或Google。</p>
<hr>
<p>一个风和日丽的劳动节假期。</p>
<p>某个技术团队需要解决一个<strong>小问题</strong>，他们想让<strong>用Unity3D开发的工程读取串口数据</strong>。<del>作为物联网领域非常会吹牛逼的</del>潘老师闻之欣然规往。在飚到四轮离地的公交车上，老潘已经在网上找到了<strong>C#读取串口数据</strong>的现成轮子，可以直接使用，所以这确实是一个小问题。</p>
<p>然而，由于Unity3D的软弱性，C#的一些标准库函数不能使用。在Windows系统上运行毫无问题的代码放进U3D工程中就完全没法用。例如，以下语句大致作用是捕获每次串口数据并触发括号里那个事情，这个事情是判断目前这个命令下该干什么，这条是程序不断读取串口并且根据命令执行不同动作的<strong>关键</strong>一步：</p>
<pre><code class="language-C">my_port.DataReceived += newSerialDataReceivedEventHandler(DoSomething)
</code></pre>
<p>然而很巧地，U3D不支持这种做法，如果想让程序接到串口命令后，根据命令执行不同的U3D动作，使用这条代码是行不通的。</p>
<p>潘老师看到甲方团队已经写好的程序结构是类似这样的。</p>
<pre><code class="language-C">void Start() {
    // 写了点什么代码在这里
}
void Update() {
    // 写了点什么代码在这里
}
</code></pre>
<p>那好，潘老师猜测这个工程运行后<strong>从Start()开始</strong>，从网上找到了U3D下能够使用的读串口的代码后，潘老师使用线程启动串口读取的动作，启动的线程放在Start()里面。</p>
<pre><code class="language-C">my_thread = new Thread(receive_thread);
my_thread.Start();
</code></pre>
<p>再创建一个receive_thread()的函数，这里面放一个while循环，while里不断循环重复尝试从串口数据中读出来命令。这样一旦串口数据更新，字符串receive_str也会立刻跟进（但愿）。</p>
<pre><code class="language-C">void receive_thread() {
    while(true) {
        Stringreceive_str = my_port.ReadLine();
    }
}
</code></pre>
<p>还是不行，发现进度会卡在while循环里面，没有任何反应，用log输出一下（C#里的相当于print的办法）也不出任何结果。后来查证了一下才发现：循环里用的**ReadLine()**虽然可以在U3D里面使用，但是这个函数是接收一串字符串，在开始执行接收数据后，<strong>只有遇到换行标识才会结束接收并形成一个串</strong>。然而很巧地，串口来的数据是从一个开发板来的，这串口数据源源不断根本不给换行标识。于是整个while循环其实并没有在循环，只是卡在了ReadLine()一直在等一个根本等不到的换行符。</p>
<p>好，那就用个原始的办法吧，那就让串口的原生数据给程序，通过程序来解码字符串。好在网上有使用List等复杂方法接受数据并且形成字符串的现成轮子，拿来借用一下。所需的只是每条串口数据的<strong>起始标志和结束标志</strong>，即可不用换行符也能得到一条完整正确的串口数据。</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597760278799.png" alt="" loading="lazy"></figure>
<p>然而很巧地，潘老师居然找不到什么明显的起始标志和结束标志，这就导致不能清晰的判断出一条数据什么时候开始什么时候结束。</p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597761262982.png" alt="" loading="lazy"></figure>
<p>探究了半天，发现大多数命令的起始标志还是一致的，那就以起始标志作为分割语句的标志吧</p>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597761269267.png" alt="" loading="lazy"></figure>
<p>分割完之后，拿着一条得到的原生数据，做一下转码。好在这些码只是字符串对应的ASCII码值，还是很容易转一下的。</p>
<pre><code class="language-c">string ascii_to_str(byte[] com_data) {
    return System.Text.Encoding.ASCII.GetString(com_data);
}
</code></pre>
<p>再从转好的字符串里提取出关键信息。甲方需要得到串口返回的命令对应ID号，串口传回来的数据类似这样：</p>
<pre><code class="language-c">Result:ID(5),Text[&quot;老潘家的潘老师&quot;]
</code></pre>
<p>这里括号中的数字5就是关键信息，使用<strong>正则表达式</strong>给他拿出来：</p>
<pre><code class="language-c">Regex rgx = newRegex(@&quot;Result:ID(([\s\S]*?)),Text&quot;);
Match match = rgx.Match(indata);  // indata是需要识别的字符串
string result_id = match.Groups[1].Value;
</code></pre>
<p>但是预想他只拿出括号里的数字，结果得到的是一个带括号的（5），但是没有时间纠结这个，至少有关键信息可以进行下一步了。这里可能是正则的括号问题，是不是应该转义一下？</p>
<p>在使用这个命令号进行判断的时候，使用了会被人嘲笑的最简陋的if列表：</p>
<pre><code class="language-c">if (result_id == &quot;(0)&quot;) {
    // 0号命令需要执行的代码
}
if (result_id == &quot;(1)&quot;) {
    // 1号命令需要执行的代码
}
if (result_id == &quot;(2)&quot;) {
    // 2号命令需要执行的代码
}
</code></pre>
<p>在if列表里面填具体的U3D动画时，运行报错<strong>动画必须放在主线程</strong>，就是Update()中。不能放在这个另扯出来的自定义线程里。经过尝试动画需要<strong>放在Update()里面</strong>。</p>
<p>（以下是猜测，如果错误请指出）这时候潘老师突然想到，U3D的机制可能是，在Update()里面进行工程运行的大循环，这个大循环的大概作用机制是不断刷新屏幕来更新屏幕上的像素信息。但凡编写程序涉及到动画，目前见到的解决方法基本都是通过循环刷新来做的。刷新的速度单位是帧，或者fps。刷新不仅可以对屏幕刷新，还可以对键盘鼠标等设备刷新，提高接受信号的灵敏度**（所以玩游戏的还是fps越高越好）**。</p>
<p>这时候就陷入了一个问题，接收串口信息的这个线程本身其实就是一个死循环，如果进入到这个线程就势必没法往下进行，不能往下进行就没办法循环起来刷新动画的这个事情了。</p>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597761281223.png" alt="" loading="lazy"></figure>
<p>那看来需要改一下，不在Start()里运行读串口的死循环线程，而改为在Update()里，随着整体的刷新来不停执行这个线程，而线程自己不循环，只执行一次。整体上看，还是相当于一直在循环这个线程的功能。而且还可以直接把判断写在Update()里面，当识别到不同的数字，执行不同的动作。再者，线程返回值好像做起来挺麻烦，那干脆就把存数字的字符串弄成public变量，每次循环，这个变量就更新，if列表也接受这个值进行判断。潘老师的智慧闪闪发光。</p>
<figure data-type="image" tabindex="5"><img src="https://divertingPan.github.io/post-images/1597761287879.png" alt="" loading="lazy"></figure>
<p>这么做当然是不可以的了。虽然想法没什么问题，但是U3D在某种玄学力量的作用下，导致这个<strong>Update()的循环速度</strong>根本不够。串口偶尔会一下子给很多条数据，但Update()循环一次只能读一条数据，就会导致堵车。结果就是，一条命令已经发出了很久，屏幕上的动画才开始播放。</p>
<figure data-type="image" tabindex="6"><img src="https://divertingPan.github.io/post-images/1597761295259.png" alt="" loading="lazy"></figure>
<p>while循环读取串口数据的速度倒是很快，基本和串口发送数据的速度一致，但是因为跳不出来循环导致没法往下进行，也是没办法用的。</p>
<figure data-type="image" tabindex="7"><img src="https://divertingPan.github.io/post-images/1597761302136.png" alt="" loading="lazy"></figure>
<p>一筹莫展，潘老师掉了半斤头发，这时候甲方有人提醒我，给while加一个判断条件，判断到那个括号里的命令号被识别出来之后，<strong>跳出快的while循环</strong>，进入慢循环播动画，不就搞定了吗。是的，潘老师果然还是头发多太年轻。</p>
<figure data-type="image" tabindex="8"><img src="https://divertingPan.github.io/post-images/1597761311695.png" alt="" loading="lazy"></figure>
<p>最后一步还是没能平稳的走完。虽然进入大循环里的小循环里成功的判断出了命令号到来，也跳出了小循环进行下一轮的大循环，但是动画突然开始鬼畜，不停地很快地播放动画的前一秒的瞬间。</p>
<figure data-type="image" tabindex="9"><img src="https://divertingPan.github.io/post-images/1597761318152.png" alt="" loading="lazy"></figure>
<p>因为用的List存储了串口来的原生数据，小循环识别命令号成功并且结束循环的时候没有把List里的这条数据清空，结果导致下一轮大循环来到，进入小循环后直接这条信息就又被识别出命令号并且结束了小循环，触发了动画。懂了吗？</p>
<figure data-type="image" tabindex="10"><img src="https://divertingPan.github.io/post-images/1597761327026.webp" alt="" loading="lazy"></figure>
<p>反正最终，潘老师<strong>用了一天，为甲方解决了一个小问题</strong>。坐在飞翔的公交车上，潘老师给甲方发了一条消息：</p>
<blockquote>
<p>“今天那程序，即使你觉得哪里写的不好，也不要轻易改。不然”</p>
</blockquote>
<figure data-type="image" tabindex="11"><img src="https://divertingPan.github.io/post-images/1597761332917.gif" alt="" loading="lazy"></figure>
<hr>
<p>附：一个非常简易的不用goto的跳出双层循环的python示例。如果不加最后三行else，continue，break每次就只能跳出里面那层while。while和for都可以这样用甚至两个循环混着也能用。</p>
<pre><code class="language-python">i = 0
while True:
    while True:
        i += 1
        print(i)
        if i == 4:
            break
    else:
        continue
    break
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[猎奇 | Python+ADB实现自动点赞和抽奖检测]]></title>
        <id>https://divertingPan.github.io/post/auto_lottery/</id>
        <link href="https://divertingPan.github.io/post/auto_lottery/">
        </link>
        <updated>2018-08-10T12:39:21.000Z</updated>
        <content type="html"><![CDATA[<h1 id="为什么要做这个呢">为什么要做这个呢？</h1>
<p>在各类直播平台，都有对主播点赞的功能，有一些直播平台（例如淘宝直播）的点赞按钮是可以无限点击的（为什么要双击666，明明可以无限击）。在淘宝直播中，主播看到自己收获很多赞之后，有概率放一些粉丝福利，某些福利是以抽奖为形式的，粉丝们在互动区疯狂发送关键字，主播随机截图，并给在截图中的粉丝送出福利。这个过程需要粉丝疯狂的点赞，以及实时盯着屏幕以防什么时候开始刷屏抽奖。如果全程亲自操作，依靠自己手动点击的话，不能解放双手，是非常拉底做事效率的。如果依靠自己盯着屏幕看，也是非常原始的做法。</p>
<p>所以，如果能有一个自动点赞+自动窥屏的外挂将会让我们在薅羊毛的路上满载而归。</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597668269074.png" alt="" loading="lazy"></figure>
<p>鉴于之前已经有同学实现了自动挖掘抖音美女的案例，所以这个想法终于有了一丝付诸实践的曙光，潘老师和这位同学一样使用了Python+ADB的方式来实现。</p>
<p>Python大家都很熟悉，人生苦短，我用Python。</p>
<p>ADB（Android Debug Bridge）则是一种通过电脑调试控制安卓设备的技术，我可以在电脑上输入指令，达到和手工操作一样的效果（比如说我可以用电脑控制安卓手机点开某个应用，点击某个按钮等等）。仿佛开了外挂一样舒爽。</p>
<p>（前期准备工作需要安装：Python环境，opencv，pillow，ADB并配置好环境变量，免费注册一个百度文本识别的账号）</p>
<h1 id="自动点赞">自动点赞</h1>
<h2 id="获取点击的位置">获取点击的位置</h2>
<p>我想知道点击了屏幕之后，这个点的位置是多少，该如何操作？</p>
<p>首先连接电脑与手机，手机打开USB调试，接着在电脑上打开cmd输入</p>
<pre><code>adb shell getevent
</code></pre>
<p>这时cmd会等待点击。</p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597668366988.PNG" alt="" loading="lazy"></figure>
<p>我点击了键盘上a字母的位置，cmd给出了以下信息。找到下面的信息，最后括号位置的十六进制数就是坐标，换算成十进制即可。</p>
<pre><code>/dev/input/event4: 0003 0035 (X坐标)
/dev/input/event4: 0003 0036 (Y坐标)
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597668416399.PNG" alt="" loading="lazy"></figure>
<h2 id="自动点赞-2">自动点赞</h2>
<p>首先，ADB语句控制点击屏幕上某一点的指令是：</p>
<pre><code>adb shell input tap 123 456
</code></pre>
<p>其中的123 456是点击位置的坐标，这个坐标可以通过上一节方式获取。经过测试，淘宝直播的右下角点赞按钮坐标大概是(1015, 1730)。</p>
<p>为了避免让淘宝觉得这个操作太机器人，所以不让他每次都点击到这个固定点，让他随机出现一个偏移（虽然仔细想想好像这样做也没什么意义）。</p>
<p>为了一直点下去，加一个循环进去，使用for循环，可以指定给主播点多少个赞。同样地，为了不让淘宝觉得点击速度太均匀，加入一个随机的延迟。点赞主要的部分就是如下代码了：</p>
<pre><code class="language-python">import os
import time
import random

def click_hearts(i):
    delay = random.uniform(0.1, 0.5)
    time.sleep(delay)
    randomX = str(random.randint(-15, 15) + 1015)
    randomY = str(random.randint(-20, 20) + 1730)
    cmd = 'adb shell input tap ' + randomX + ' ' + randomY
    os.popen(cmd)
    print('%s%d, %s%f%s, %s(%s,%s)'%('已点赞 X', i, '延迟', delay, 's',
          '点击坐标：', randomX, randomY))
          
for i in range(1,21): # 自动点赞20次
    click_hearts(i)
</code></pre>
<p>实际测试一下，我们先使用这个方法点一点键盘上的a字母看看：</p>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597668597318.PNG" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="5"><img src="https://divertingPan.github.io/post-images/1597668625878.png" alt="" loading="lazy"></figure>
<p>嗯还是可以的。</p>
<h1 id="检测有没有抽奖">检测有没有抽奖</h1>
<h2 id="获取屏幕">获取屏幕</h2>
<p>ADB截取屏幕的指令是：</p>
<pre><code>adb shell screencap -p /sdcard/autolottery.png
</code></pre>
<p>其中-p后面接的是存放的路径和文件名。</p>
<p>ADB把文件从手机中拷贝出来的指令是：</p>
<pre><code>adb pull /sdcard/autolottery.png ./img
</code></pre>
<p>第一个路径是手机中文件的路径和文件名，后一个路径是存放在电脑中的路径，./img表示存在当前py文件目录下的img文件夹里。</p>
<p>在python中给Android发送ADB指令则通过调用系统cmd实现，让python帮你把引号里面的句子输进cmd并且执行：</p>
<pre><code class="language-python">def get_screen():
    os.system('adb shell screencap -p /sdcard/autolottery.png')
    os.system('adb pull /sdcard/autolottery.png ./img')
</code></pre>
<h2 id="截图预处理">截图预处理</h2>
<p>由于直接拿来截图进行文本识别，正确率较低（经过测试实际是非常低了），所以需要对源图像处理一下。这里需要安装opencv和pillow。</p>
<p>首先裁剪出文本区域，尽量去除干扰。</p>
<pre><code class="language-python">import cv2
from PIL import Image

def cut_image():
    # 裁出文字识别区
    image = Image.open('img/autolottery.png')
    box1 = (0, 1100, 800, 1700) #设置图像裁剪区域(left, upper, right, lower)
    image1 = image.crop(box1)
    image1.save('img/textarea.png')
</code></pre>
<p>然后将该区域二值化，提升识别率，由于要识别的文字部分颜色是纯白，所以阈值可以设的大一些：</p>
<pre><code class="language-python">def extract_text():
    # 图像分割
    image = cv2.imread(&quot;img/textarea.png&quot;)
    image = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY) # 转灰度图
    (T, threshInv) = cv2.threshold(image, 230, 255, cv2.THRESH_BINARY_INV) # 反阈值化，阈值为215
    cv2.imwrite('img/textextract.png', threshInv)
</code></pre>
<p>原图：</p>
<figure data-type="image" tabindex="6"><img src="https://divertingPan.github.io/post-images/1597668738119.png" alt="" loading="lazy"></figure>
<p>处理后：</p>
<figure data-type="image" tabindex="7"><img src="https://divertingPan.github.io/post-images/1597668750764.png" alt="" loading="lazy"></figure>
<h2 id="文本识别">文本识别</h2>
<p>二话不说，直接怼百度的文本识别。百度毕竟是汉语起家，识别汉语的准确度还是很高的。</p>
<pre><code class="language-python">from aip import AipOcr
# 配置百度AipOcr
APP_ID = '自己去注册'
API_KEY = '自己去注册'
SECRET_KEY = '自己去注册'

client = AipOcr(APP_ID, API_KEY, SECRET_KEY)

def baidu_ocr_text():
    # 百度文本识别AipOcr
    image = open('img/textextract.png', 'rb').read()
    msg = client.basicGeneral(image)
    text = 'result:\n'
    for i in msg.get('words_result'):
        text += (i.get('words') + '\n')
    print(text)
    return text
</code></pre>
<p>看一下识别上面图片的结果：</p>
<figure data-type="image" tabindex="8"><img src="https://divertingPan.github.io/post-images/1597668788329.PNG" alt="" loading="lazy"></figure>
<p>在识别简体中文的同时，不耽误识别数字，实际上识别中文的同时英文字母也可以识别但是准确率低一些。</p>
<h2 id="字符串与子串">字符串与子串</h2>
<p>这种基本算法<del>（虽说潘老师算法很渣）</del>就不需要解释很多了，送分题。</p>
<pre><code class="language-python">def string_lottery(m_str, sub_str):
    count = 0
    # 第一层循环，从主字符串的第0个元素开始
    # 第二层循环，通过切片获取下标从i开始与子字符串长度一致的字符串，并与字符串比较，如果等于子字符串则count+1
    for i in range(len(m_str)-1):
        if m_str[i:i+len(sub_str)] == sub_str:
            count += 1
    return count
</code></pre>
<h2 id="组装">组装</h2>
<p>当我们有了获取屏幕，并且识别其中文本的能力，那就让他自动为我们检测有没有出现抽奖契机，根据经验，如果屏幕中有4条以上抽奖关键字，证明抽奖要开始了，大家已经躁动起来了，所以要让刚刚识别出的那串文本里出现“指定关键字”这个子字符串数量大于等于4即可。</p>
<p>直接怼，不解释：</p>
<pre><code class="language-python">import ctypes

while 1:
    get_screen()
    cut_image()
    extract_text()
    text = baidu_ocr_text()
    string_count = string_lottery(text, &quot;抽奖&quot;)
    if string_count &gt;= 4:
        ctypes.windll.user32.MessageBoxW(0, '要抽奖了，关键词出现次数：'
             + str(string_count), '抽奖了', 0)
        break
</code></pre>
<p>因为目前时间主播还没有开播，所以先自己拿备忘录打几个字试试。</p>
<figure data-type="image" tabindex="9"><img src="https://divertingPan.github.io/post-images/1597668850996.png" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="10"><img src="https://divertingPan.github.io/post-images/1597668857900.PNG" alt="" loading="lazy"></figure>
<h1 id="实际测试">实际测试</h1>
<p>可悲的事情发生了，测试的时候发现主播已经调整了抽奖方式，多数奖品用随机放出的“狂戳福利按钮”送出了，晚上只出了一次刷屏抽奖的方法。所以，这件事情告诉我们，<strong>产品研发就是在和时间赛跑。</strong></p>
<p>所以，下次开发自动检测“狂戳福利按钮”并自动狂戳的外挂？</p>
<p>实际测试效果（关键字：“有草”）：</p>
<figure data-type="image" tabindex="11"><img src="https://divertingPan.github.io/post-images/1597668904469.png" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="12"><img src="https://divertingPan.github.io/post-images/1597668909990.PNG" alt="" loading="lazy"></figure>
<p>在这个外挂的辅助下，潘老师还是没有抽到奖，也许这就是，非命不改。</p>
<figure data-type="image" tabindex="13"><img src="https://divertingPan.github.io/post-images/1597668943560.jfif" alt="" loading="lazy"></figure>
<h1 id="总结">总结</h1>
<p><del>运气差这种事，用python都拯救不了。</del></p>
<p>其实单看这个外挂还是有很多成长空间的，例如：</p>
<p>【点赞】可以先检测用户点击的坐标，接受并传递给代码里的坐标。节省事先查坐标的时间和精力。</p>
<p>【抽奖】可以改造关键字的部分，不需要手动指定关键字，智能识别刷屏的字符串。再者还可以添加自动打字参与刷屏，以及自动检测抽奖结果，实现全自动无人值守式抽奖。</p>
<p>项目完整版的地址：<a href="https://github.com/divertingPan/Auto_Lottery">GitHub</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[数字图像处理 | 图像二值化中的规则抖动算法]]></title>
        <id>https://divertingPan.github.io/post/bayer_dither/</id>
        <link href="https://divertingPan.github.io/post/bayer_dither/">
        </link>
        <updated>2018-06-29T10:52:10.000Z</updated>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>在只有黑色墨水的印刷机上只能搞出黑白的图形，那么问题就来了，印刷机里并没有灰色的墨，图片里的灰色阴影是怎么弄出来的？</p>
<p>印刷机处理灰色的方式，是通过控制区域内小墨点的密度来实现的。很好理解，如果不印任何黑点，这个区域就是白色，如果印满黑点，这个区域就是黑色，自然地，我们能推理出：印的黑点越密，显得颜色越深。在老式的报纸上能观察到这种效应。</p>
<figure data-type="image" tabindex="1"><img src="https://divertingPan.github.io/post-images/1597661759495.jpg" alt="" loading="lazy"></figure>
<center style="font-size:14px;color:#AAAAAA">报纸上的图片的放大，这张报纸是通过控制墨点的大小来控制密度的</center>
<p>如果墨点的大小是不变的，通过改变区域内墨点的数量，来使人看到不同的灰度，这种方法叫做图像抖动。而计算显出来的灰度和打印墨点数量之间关系的算法（抖动算法）主要有规则抖动和误差扩散抖动，由于通过规则抖动会使图像出现一些有意思的情况，故潘老师这次主要研究的是规则抖动算法。</p>
<p>这次写的东西在网上能找到的参考资料比较匮乏，潘老师通过自己尝试以及瞎猜，总结了一下，并且得出了一些没什么用的结果。对算法、数学恐惧或是不想动脑思考的同学可只看图片。</p>
<h1 id="计算过程">计算过程</h1>
<p>规则抖动，顾名思义，可以理解为他的抖动方法是有一定规则的。规则就来自于Bayer抖动表。至于为什么叫<strong>Bayer</strong>抖动表，潘老师也不太清楚，只知道仿佛<strong>并不是</strong>叫Bayer的人发明的这个方法。这是Bayer抖动表的计算过程：<br>
<img src="https://divertingPan.github.io/post-images/1597662259033.PNG" alt="" loading="lazy"></p>
<figure data-type="image" tabindex="2"><img src="https://divertingPan.github.io/post-images/1597662263985.PNG" alt="" loading="lazy"></figure>
<p>通常大家会取M<sub>3</sub>作为Bayer抖动表。为了不想看见数学式子直观的看到Bayer抖动表里面到底是什么样的，潘老师让MATLAB计算了一下M<sub>3</sub>：<br>
<img src="https://divertingPan.github.io/post-images/1597662391052.PNG" alt="" loading="lazy"></p>
<p>用大白话解释规则抖动算法的想法：<em>给你一个点的灰度值，在这个点的位置上要不要打印墨点？你这时候就去立刻拿来Bayer抖动表，从里面随机挑选一个数，如果挑到的数大于这个点的灰度值，就打上墨点，不然就不打。</em>（关于灰度值的解释：灰度值越高，就越趋于白色。灰度值越低，越趋于黑色。）</p>
<p>这样会发生什么事呢？假设给我一个区域，这里面大多是<strong>很深的颜色</strong>（假如灰度值是10），那么我均匀地随机在Bayer抖动表里挑到的数，大部分（从11到63）都会大于这个<strong>很深的颜色</strong>（10），结果就会导致大部分点都被打上墨点，呈现的视觉效果确实是<strong>很深的颜色</strong>。</p>
<p>如果考虑极端情况。给我的一个区域是纯黑色。那么我在这个表里，不管怎样取数，都会大于我手里的纯黑色，所以导致，每个点都打墨点，所以就铺满了墨点就呈现黑色了。同理，给我一个纯白的区域，我在这个表里，不管怎么取数都会小于纯白色的灰度值，所以总是没有墨点打在纸上，就呈白色了。</p>
<p>算法思想非常简单，但是具体实现算法的时候，我们用另一种方法在抖动表里取值，方法是：用图像里的点的（x，y）坐标与抖动表的尺寸取余。具体操作的过程大概就是，随着我在图片上取点的位置一格一格移动，我在抖动表里取数的位置也在移动。对于一个足够大的区域来说，这个区域呈现的色彩可以均匀的被抖动表里所有元素“判断”一遍，在宏观上呈现预期的概率分布。通过这种方法抖动出来的图是很有特点的，如下图。也正因如此，他叫做规则抖动。<br>
<img src="https://divertingPan.github.io/post-images/1597663002331.png" alt="" loading="lazy"></p>
<figure data-type="image" tabindex="3"><img src="https://divertingPan.github.io/post-images/1597663011546.png" alt="" loading="lazy"></figure>
<center style="font-size:14px;color:#AAAAAA">灰阶图以及其规则抖动图，一个方格代表一个像素点。为了观察方便，图片被放大了</center>
<p>还有一点，因为实际图像灰度范围是0-255，然而Bayer抖动表里的数字范围是0-63，所以对源图像还要进行范围的压缩。以下是使用MATLAB进行抖动表的生成和抖动的算法，供学有余力想要研究技术细节的同学参考。需要完整代码的请<a href="https://github.com/divertingPan/Bayer_Dither">前往github</a></p>
<pre><code class="language-python">m1 = [[ 0 2 ];
      [ 3 1 ]];

u1 = ones(2, 2);

m2 = [[ 4*m1       4*m1+2*u1 ];
      [ 4*m1+3*u1  4*m1+u1   ]]

u2 = ones(4, 4);

m3 = [[ 4*m2       4*m2+2*u2 ];
      [ 4*m2+3*u2  4*m2+u2  ]]

OuputM3 = 0;
for i = 1:height
    for j = 1:width
        ImageColor = GrayImage(i,j) / 4;
        BayerMatrix = m3(bitand(i,7) + 1, bitand(j,7) + 1);
        if ( ImageColor &lt;= BayerMatrix )
           OuputM3(i,j) = 0;
        else
           OuputM3(i,j) = 255;
        end
    end
end
Image8BitM3 = uint8(OuputM3);
</code></pre>
<p>当然，使用M<sub>4</sub>的Bayer抖动表就不需要压缩取值范围了，M<sub>4</sub>的抖动表可以做到一个实际的灰度值对应一种取值概率，即一种抖动花纹，我们也可以称其为标准图案，即每种灰度值所对应的黑白点拼出来的图案。但是实际上用M<sub>3</sub>已经足够，所以下文均以M<sub>3</sub>作为抖动表。<br>
<img src="https://divertingPan.github.io/post-images/1597663252226.png" alt="" loading="lazy"></p>
<center style="font-size:14px;color:#AAAAAA">4阶抖动表与3阶抖动表生成的结果相差不大</center>
<figure data-type="image" tabindex="4"><img src="https://divertingPan.github.io/post-images/1597663316482.jpg" alt="" loading="lazy"></figure>
<center style="font-size:14px;color:#AAAAAA">实际图像</center>
<figure data-type="image" tabindex="5"><img src="https://divertingPan.github.io/post-images/1597663343206.jfif" alt="" loading="lazy"></figure>
<center style="font-size:14px;color:#AAAAAA">实际图像应用抖动算法的效果（已重新缩放）</center>
<h1 id="多折腾折腾">多折腾折腾</h1>
<p>以上是图像中只含有黑白点（二值）的情况，接下来考虑多值的（以4个值为例——白，浅灰，深灰，黑）规则抖动。潘老师没有查找到相关的资料，干脆直接用了最简单粗暴的方法，就是，将Bayer抖动表通过运算处理成3个取值域）(0-21，22-43，44-63)，将图形色彩也划分为对应的4个取值域，分别在各自域内做规则抖动。经过测试证明这个方法还是可以接受的。</p>
<figure data-type="image" tabindex="6"><img src="https://divertingPan.github.io/post-images/1597663932594.png" alt="" loading="lazy"></figure>
<center style="font-size:14px;color:#AAAAAA">二值规则抖动和4值规则抖动的对比，在使用同样的Bayer抖动表的基础上，4值过渡更加平缓</center>
<figure data-type="image" tabindex="7"><img src="https://divertingPan.github.io/post-images/1597663984180.jfif" alt="" loading="lazy"></figure>
<center style="font-size:14px;color:#AAAAAA">实际图像应用的效果</center>
<p>算法见代码部分。需要完整代码的请<a href="https://github.com/divertingPan/Bayer_Dither">前往github</a></p>
<pre><code class="language-python">output = zeros(height, width);
for i = 1:height
    for j = 1:width

        BayerMatrix1 = m3(bitand(i,7) + 1, bitand(j,7) + 1) / 3;
        BayerMatrix2 = 21.3333 + BayerMatrix1;
        BayerMatrix3 = 21.3333 + BayerMatrix2;
        ImageColor = GrayImage(i,j) / 4;

        if (ImageColor &gt;= 0 &amp;&amp; ImageColor &lt; 21)
            if (ImageColor &gt; BayerMatrix1)
                output(i,j) = 84;
            else
                output(i,j) = 0;
            end
        elseif (ImageColor &gt;= 21 &amp;&amp; ImageColor &lt; 43)
            if (ImageColor &gt;= BayerMatrix2)
                output(i,j) = 171;
            else
                output(i,j) = 84;
            end
        else
            if (ImageColor &gt;= BayerMatrix3)
                output(i,j) = 255;
            else
                output(i,j) = 171;
            end
        end
    end
end
</code></pre>
<p>老师，能再给力点吗？</p>
<figure data-type="image" tabindex="8"><img src="https://divertingPan.github.io/post-images/1597664079642.jpg" alt="" loading="lazy"></figure>
<p>潘老师知道彩色图像是由RGB三个通道组成的，每个通道实际上都是类似灰度的表达方式，但是通过某种神力，三个通道的数值合成后就成为了我们能看到的彩色。如果分别对三个通道做抖动。那么R、G、B三色就分别只有0和1的取值，排列组合，会出现8种颜色。</p>
<figure data-type="image" tabindex="9"><img src="https://divertingPan.github.io/post-images/1597664107520.png" alt="" loading="lazy"></figure>
<p>用一个实际的彩色图像看看效果。（算法和之前的都类似，不再列出）</p>
<figure data-type="image" tabindex="10"><img src="https://divertingPan.github.io/post-images/1597664137382.webp" alt="" loading="lazy"></figure>
<p>老师，能再给力点吗？</p>
<figure data-type="image" tabindex="11"><img src="https://divertingPan.github.io/post-images/1597664079642.jpg" alt="" loading="lazy"></figure>
<p>若是对RGB三个通道也分别进行4值抖动，那么可获得的色彩范围就更多了（4×4×4=64种），颜色也就更加准确了，算法也与灰度4值抖动相仿。</p>
<figure data-type="image" tabindex="12"><img src="https://divertingPan.github.io/post-images/1597664203730.webp" alt="" loading="lazy"></figure>
<p>虽然放大来看图像上会有条块状纹理出现，但如果远观的话这幅图的细节已经还原出很多了。抖动算法最主要的应用价值在于他可以极大的压缩图像存储空间，主要是因为<strong>色彩空间被压缩了</strong>，原本需要256个数值表达的灰度图我甚至可以只用黑白两色表达。（在均采用PNG格式编码的条件下，测试用的原图798KB，RGB通道二值化后95.8KB，RGB通道4值化后148KB，压缩效果明显）当然如果合理看待这些条块的话，可以把他看作一种比较独特的滤镜（这样说的话，Bayer抖动表就是一种滤波器）。</p>
<figure data-type="image" tabindex="13"><img src="https://divertingPan.github.io/post-images/1597664376195.png" alt="" loading="lazy"></figure>
<p>【注】写推送使用的配图都是低分辨率然后放大过的，为的是各位观察方便。实际使用代码生成的图片中，上面那些图里的一个小方格就是一个图片像素点。</p>
]]></content>
    </entry>
</feed>